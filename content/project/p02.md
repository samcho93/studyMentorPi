---
id: p02
track: project
title: QR 지시형 실내 배송 로봇
duration: 240
level: 중급
requires: [a02, m11, m13, r04]
tools: [sim3d, sim, playground]
---

## 학습 목표

- 배송 로봇의 요구사항을 기능·비기능·안전 한계로 나누어 정의하고, 이를 서비스·액션·토픽 인터페이스와 배송 상태 기계로 설계합니다.
- 스테이션 카메라로 QR 지시서를 읽어 `DELIVER:<목적지>` 형식을 검증하고, 목적지 조회표(이름 → map 자세)로 Nav2 목표를 만듭니다.
- 도착을 두 번 확인합니다: 로봇 쪽(Nav2 결과 + TF 위치 오차)과 사람 쪽(버튼·서비스 수령 확인, 대기 시간 초과).
- 주행 실패·막힘·시간 초과를 감지해 코스트맵 지우기 → 재시도 → 대체 경로(재계획) → 포기·복귀 순서로 복구합니다.
- Playground에서 상태 기계와 복구 전략을 먼저 검증하고, MentorPi 실물에서 `navigation.launch.py` + 카메라로 같은 구조를 실행합니다.

## 프로젝트 개요와 요구사항

연구실 입구의 **스테이션(도크)**에 서 있는 로봇에게 사람이 QR 지시서를 보여 주고 "배송 요청"을 누르면, 로봇이 지시서에 적힌 방까지 물건을 싣고 가서 받는 사람의 확인을 기다렸다가 스테이션으로 돌아옵니다. 길이 막히면 스스로 다른 길을 찾고, 끝내 못 가면 포기하고 돌아와 보고합니다.

QR 인식(A02), Nav2 목표 전송(M11·M13), 서비스·액션(R04)을 하나로 묶는 것이 핵심입니다. 특히 **"실패했을 때 무엇을 하는가"**를 설계하는 것이 이 프로젝트의 절반입니다.

### 팀 구성 (4명 기준)

| 역할 | 담당 | 산출물 |
|---|---|---|
| 인식 | QR 형식 정의, 인쇄물 제작, 인식 노드 | 지시서 QR 세트, 인식률 측정표 |
| 내비 | 지도, 목적지 조회표(주·대체 접근점), Nav2 설정 확인 | `DEST` 표, 경로 사진 |
| 상태 기계 | `delivery_bot` 노드, 서비스·복구 로직 | 상태 기계 코드, 인터페이스 정의 |
| 시험·안전 | 고장 주입 시나리오, 적재물 고정, 발표 | 시험 결과표, 안전 체크리스트 |

### 기능 요구사항

| ID | 요구사항 | 확인 방법 |
|---|---|---|
| F1 | `/delivery/request`(서비스) 호출 시 스테이션 카메라의 최신 QR(2 s 이내)을 읽어 접수·거절을 즉시 응답한다 | 응답 `success`, `message` |
| F2 | QR은 `DELIVER:<이름>` 형식만 받고, 조회표에 없는 이름·다른 형식은 거절한다 | 잘못된 QR 3종 시험 |
| F3 | 배송 중 새 요청은 거절한다(한 번에 한 건) | 주행 중 요청 |
| F4 | 목적지 도착은 Nav2 `SUCCEEDED` **그리고** TF 위치 오차 ≤ 0.3 m일 때만 인정한다 | 로그의 오차 값 |
| F5 | 도착 후 수령 확인(보드 버튼 또는 `/delivery/confirm`)을 기다리고, 60 s가 지나면 "미수령"으로 복귀한다 | 두 경우 모두 시험 |
| F6 | 실패 시 2회째는 코스트맵을 지우고 재시도, 3회째는 대체 접근점으로 재계획, 그래도 실패하면 포기하고 복귀한다 | 통로 막기 시험 |
| F7 | 모든 상태 변화를 `/delivery/status`로 알리고 LED·부저로 표시한다 | `ros2 topic echo` |

### 비기능 요구사항

| ID | 항목 | 목표값 |
|---|---|---|
| N1 | QR 인식 | 30~60 cm 거리, 정면 ±30°에서 10회 중 9회 이상 |
| N2 | 응답성 | 요청 서비스 응답 0.5 s 이내 (주행은 비동기) |
| N3 | 주행 시간 초과 | 목표 하나당 90 s (경로 길이 ÷ 0.2 m/s의 2~3배) |
| N4 | 도착 정확도 | 목표와 실제 위치 차 평균 ≤ 0.2 m |
| N5 | 연속 운용 | 배송 10건 연속, 사람 개입 없이 모두 IDLE로 복귀 |

### 안전 한계

| 항목 | 값 | 이유 |
|---|---|---|
| 선속도 · 각속도 | ≤ 0.2 m/s, ≤ 0.5 rad/s | Nav2 설정 유지 (CLAUDE.md 안전 원칙) |
| 적재물 | 가볍고 낮은 물건만, 테이프로 고정. **액체·유리 금지** | 급정지·회전 시 낙하 |
| 수령 대기 | 도착 지점에서 정지, 모터 명령 없음 | 사람이 물건을 꺼내는 동안 움직이지 않음 |
| 재시도 상한 | 목표당 3회 | 무한 재시도로 같은 곳에서 계속 부딪히지 않게 |
| 복귀 실패 | SAFE_STOP + 노랑 LED + 경고음 | 운영자 호출 |

:::info 수업 시간 배분 (4교시)
1교시: 요구사항·인터페이스·상태 기계 설계 + Playground 실습 1 / 2교시: M1 목적지 조회표 + M2 QR 접수 / 3교시: M3 배송·복귀 + M4 복구 / 4교시: M5 고장 주입 시험, 평가, 발표.
:::

## 시스템 설계

### 인터페이스와 노드 그래프

@fig[p02-graph] QR 배송 로봇의 ROS 2 그래프. 요청과 수령 확인은 서비스로, 긴 주행은 Nav2 액션으로 처리합니다.

**"배송 요청"을 무엇으로 만들까?** R04에서 배운 세 가지 통신 방식을 요구사항에 맞춰 고릅니다.

| 방식 | 장점 | 단점 | 이 프로젝트에서 |
|---|---|---|---|
| 토픽 | 가장 단순 | 접수 여부를 돌려받을 수 없음 | 상태 방송(`/delivery/status`)에만 사용 |
| 서비스 | 즉시 접수·거절 응답 | 긴 작업의 진행 상황·취소가 없음 | `/delivery/request`, `/delivery/confirm` (`std_srvs/Trigger`) |
| 액션 | 진행 피드백, 취소, 최종 결과 | 인터페이스 패키지를 만들어야 함 | 확장: `Deliver.action` |

기본 구현은 추가 패키지 없이 돌아가도록 `std_srvs/Trigger`를 씁니다. 목적지는 요청 메시지가 아니라 **스테이션 카메라가 본 QR**에서 옵니다. 앱에서 목적지를 직접 보내거나 진행률을 받으려면 다음 인터페이스를 정의합니다(M5 심화).

```text
# delivery_interfaces/action/Deliver.action
string destination            # 비우면 스테이션 QR에서 읽음
---
bool success
string outcome                # delivered | no_receiver | aborted
uint8 attempts
float32 duration
---
string state                  # DELIVER | ARRIVED | RETURN
float32 distance_remaining
uint8 attempt
```

| 인터페이스 | 타입 | 방향 | 비고 |
|---|---|---|---|
| `/ascamera/camera_publisher/rgb0/image` | `sensor_msgs/Image` | 카메라 → bot | IDLE일 때만 2 Hz로 QR 해석 (CPU 절약) |
| `/delivery/request` | `std_srvs/Trigger` | 데스크 → bot | 응답 `accepted ROOM_A` / `rejected (...)` |
| `/delivery/confirm` | `std_srvs/Trigger` | 수령자 → bot | ARRIVED일 때만 `success=True` |
| `/ros_robot_controller/button` | `ros_robot_controller_msgs/ButtonState` | 보드 → bot | 버튼 1 누름 = 수령 확인 |
| `/navigate_to_pose` | `nav2_msgs/action/NavigateToPose` | bot → Nav2 | `BasicNavigator.goToPose()` |
| `/global_costmap/clear_entirely_global_costmap` 등 | `nav2_msgs/srv/ClearEntireCostmap` | bot → Nav2 | `clearAllCostmaps()` |
| `/delivery/status` | `std_msgs/String` | bot → 모니터 | 상태 이름 |
| `/ros_robot_controller/set_rgb`, `set_buzzer` | `RGBStates`, `BuzzerState` | bot → 보드 | 초록 IDLE, 파랑 ARRIVED, 노랑 SAFE_STOP |

:::warning 서비스 콜백 안에서 주행을 시작하지 않는다
`BasicNavigator.goToPose()`는 안에서 노드를 spin하며 목표 접수를 기다립니다. 서비스 콜백 안에서 부르면 이미 돌고 있는 실행기(executor) 안에서 다시 spin하게 되어 멈추거나 예외가 납니다. 콜백은 **상태만 바꾸고 즉시 응답**하고, 실제 `goToPose()`는 주 루프가 부릅니다.
:::

### QR 지시서 형식과 목적지 조회표

| 규칙 | 예 | 이유 |
|---|---|---|
| 접두어 `DELIVER:` | `DELIVER:ROOM_A` | 다른 용도의 QR(예: 샘플 `MentorPi`)을 명령으로 오해하지 않게 |
| 이름만, 좌표 금지 | ~~`GOTO:1.2,3.4`~~ | 인쇄물이 로봇을 임의 위치로 보낼 수 없게. 좌표는 로봇 안의 조회표가 결정 |
| 조회표에 주·대체 접근점 | `ROOM_A: [(2.0, 1.0, 90°), (2.0, 0.6, 90°)]` | 문 앞이 막혔을 때 재계획할 두 번째 목표 |
| 오류 복원 수준 M 이상, 여백 4모듈 | A02 참고 | 구겨짐·반사에 강하게 |

조회표는 M13처럼 RViz **Publish Point**(`/clicked_point`)로 좌표를 모으고, 방향(yaw)은 받는 사람이 서 있는 쪽을 보게 정합니다.

### 배송 상태 기계

@fig[p02-fsm] 배송 상태 기계. 주황 화살표가 실패·복구 경로입니다. RECOVER는 순간 상태로, 복구 동작 후 곧바로 DELIVER(또는 RETURN)로 돌아갑니다.

| 현재 | 조건 | 동작 | 다음 |
|---|---|---|---|
| IDLE | 요청 + 유효 QR | 목적지 저장, 응답 "accepted" | DELIVER (주 루프가 `goToPose`) |
| IDLE | QR 없음·형식 오류·모르는 이름 | 응답 "rejected" | IDLE |
| DELIVER | `SUCCEEDED` 그리고 위치 오차 ≤ 0.3 m | 파랑 LED, 알림음 2회 | ARRIVED |
| DELIVER | `FAILED`/`CANCELED`, 시간 초과(90 s) | 시도 +1 → 2회: `clearAllCostmaps()` 후 같은 목표, 3회: 대체 접근점 | DELIVER |
| DELIVER | 3회 초과 | 기록 "aborted" | RETURN |
| ARRIVED | 버튼·`/delivery/confirm` | 기록 "delivered" | RETURN |
| ARRIVED | 60 s 경과 | 기록 "no receiver" | RETURN |
| RETURN | 도크 도착 | 초록 LED | IDLE |
| RETURN | 3회 실패 | 노랑 LED, 경고음, 정지 | SAFE_STOP |

**왜 2회째에 코스트맵을 지우는가?** Nav2가 실패하는 흔한 이유는 "지나간 사람"이 코스트맵에 장애물로 남아 있는 경우입니다(M13의 clearing은 라이다 빔이 그 칸을 다시 지나야 일어납니다). 지우고 다시 시도하면 대부분 해결됩니다. 진짜로 막혀 있으면 두 번째도 실패하고, 그때 다른 접근점으로 **목표 자체를 바꿔** 재계획합니다. 이것은 Nav2 행동 트리의 복구(코스트맵 지우기 → spin → backup) 위에 **임무 수준의 복구**를 한 층 더 얹는 것입니다.

## 단계별 구현

### M1. 지도와 목적지 조회표

:::task M1 — 목적지 조회표 (30분)
1. M09 지도(`map_01`)를 쓰거나 배송 공간을 새로 매핑합니다.
2. `navigation.launch.py`를 켜고 목적지 2~3곳과 도크의 map 좌표를 **Publish Point**로 모읍니다. 목적지마다 대체 접근점을 하나 더 정합니다(문 반대편, 0.3~0.5 m 옆).
3. 각 좌표로 RViz **Nav2 Goal**을 보내 실제로 도달 가능한지 확인하고 `DEST` 표를 완성합니다.
:::

:::check M1 체크포인트
- [ ] 모든 접근점이 벽·가구에서 0.4 m 이상 떨어져 있고, Nav2 Goal로 도달했다.
- [ ] 도크 위치에서 2D Pose Estimate를 주는 절차를 팀원 모두가 할 수 있다.
:::

### M2. QR 접수

:::task M2 — QR 지시서와 접수 서비스 (40분)
1. Playground 실습 1로 형식 검증 함수를 완성하고, `DELIVER:ROOM_A` 등 지시서를 인쇄합니다(A02의 `qrcode_creater.py` 또는 `cv2.QRCodeEncoder`).
2. 원본 예제 `qrcode_detecter.py`로 카메라가 지시서를 읽는 거리·각도 범위를 측정합니다(N1).
3. `delivery_bot`의 `on_image`·`on_request`를 구현하고, `ros2 service call`로 접수·거절 세 경우를 시험합니다.
:::

:::check M2 체크포인트
- [ ] QR 없이 요청 → rejected, `MentorPi` QR → rejected, `DELIVER:ROOM_A` → accepted.
- [ ] 주행 중 요청 → `rejected (state=DELIVER)`.
:::

### M3. 배송과 복귀

:::task M3 — 정상 흐름 (40분)
1. 주 루프에서 DELIVER → ARRIVED → RETURN → IDLE을 구현합니다(Playground 실습 2 구조).
2. 도착 판정에 TF 위치 오차 검사를 넣습니다. `xy_goal_tolerance`(0.25 m)보다 조금 큰 0.3 m를 씁니다.
3. 버튼과 `/delivery/confirm` 두 가지 수령 확인, 60 s 대기 초과를 모두 시험합니다.
:::

:::check M3 체크포인트
- [ ] 한 건 배송 전체가 사람 개입 없이 끝나고, `/delivery/status`에 상태가 순서대로 찍힌다.
- [ ] 도착 위치 오차를 5회 기록해 평균을 구했다(N4).
:::

### M4. 실패 복구

:::task M4 — 재시도·재계획·포기 (40분)
1. Playground 실습 3으로 복구 순서를 확인한 뒤 `recover()`를 구현합니다.
2. 고장 주입: 목적지 문 앞에 상자를 놓아 막기 / 주행 중 사람이 잠깐 가로막기 / 로봇을 들어 옮기기(위치 상실).
3. 각 경우 로그에 남은 시도 횟수와 최종 결과를 기록합니다.
:::

:::check M4 체크포인트
- [ ] 잠깐 막힘 → 2회째 재시도로 성공, 문 앞 막힘 → 3회째 대체 접근점으로 성공, 완전 차단 → aborted 후 도크 복귀.
- [ ] 어떤 경우에도 같은 곳에서 무한히 재시도하지 않는다.
:::

### M5. 통합 시험과 인터페이스 확장

:::task M5 — 연속 운용 (40분)
1. 배송 10건을 연속 실행하고 건별 소요 시간·시도 횟수·결과를 표로 만듭니다(N5).
2. (심화) `delivery_interfaces` 패키지를 만들어 `Deliver.action`을 정의하고, 서비스 대신 액션 서버로 바꿔 진행 피드백과 취소를 지원합니다.
:::

## 실습 (시뮬레이션)

아래 코드는 이 페이지에서 바로 실행됩니다. ▶ 실행 후 오른쪽 결과 패널의 **출력** 탭에 로그가, **이미지** 탭에 QR 영상이 나오고, 가상 로봇을 쓰는 코드는 **2D 재생**(위에서 본 월드·라이다·궤적)과 **3D 재생**(MentorPi URDF 모델) 탭에서 자동으로 다시 볼 수 있습니다. 월드는 6 × 4 m 거실(`room`)이고, 도크는 (−0.8, −1.4), ROOM_A는 (−1.0, 0.7)입니다.

### 실습 1. QR 지시서 읽기와 검증

지시서 QR을 `cv2.QRCodeEncoder`로 만들고, 샘플 `qr.png`("MentorPi"), 기울어진 지시서, 빈 화면과 함께 인식·검증합니다.

```python run
import cv2
import numpy as np

DEST = {                                   # 목적지 조회표: 이름 → map 좌표 (x, y, yaw)
    "ROOM_A": (-1.0, 0.7, 0.0),
    "ROOM_B": (1.2, -0.7, 0.0),
    "DOCK":   (-0.8, -1.4, 0.0),
}
det = cv2.QRCodeDetector()

def make_qr(text, scale=8):                # 배송 지시서에 붙일 QR (M 수준 15% 복원, 여백 4모듈)
    p = cv2.QRCodeEncoder_Params()
    p.correction_level = cv2.QRCodeEncoder_CORRECT_LEVEL_M
    m = cv2.QRCodeEncoder.create(p).encode(text)
    m = cv2.resize(m, None, fx=scale, fy=scale, interpolation=cv2.INTER_NEAREST)
    return cv2.copyMakeBorder(m, 2 * scale, 2 * scale, 2 * scale, 2 * scale, cv2.BORDER_CONSTANT, value=255)

def parse(text):                           # "DELIVER:<목적지>" 형식만 받는다
    if not text:
        return None, "QR 없음"
    kind, _, name = text.partition(":")
    if kind != "DELIVER":
        return None, "형식 아님 (%r)" % text
    if name not in DEST or name == "DOCK":
        return None, "모르는 목적지 (%r)" % name
    return DEST[name], "목적지 %s" % name

frames = {"qr.png (샘플)": cv2.imread("qr.png")}
for t in ["DELIVER:ROOM_A", "DELIVER:ROOM_B", "DELIVER:KITCHEN"]:
    frames[t] = make_qr(t)
tilt = make_qr("DELIVER:ROOM_B")                                        # 비스듬히 본 지시서
h, w = tilt.shape[:2]
M = cv2.getPerspectiveTransform(np.float32([[0, 0], [w, 0], [w, h], [0, h]]),
                                np.float32([[30, 10], [w - 5, 40], [w - 25, h - 30], [10, h - 5]]))
frames["ROOM_B 기울임"] = cv2.warpPerspective(tilt, M, (w, h), borderValue=255)
frames["빈 화면"] = np.full((200, 200), 255, np.uint8)

for name, img in frames.items():
    text, pts, _ = det.detectAndDecode(img)
    goal, why = parse(text)
    print("%-16s → 읽음 %-18r %s" % (name, text, "목표 %s" % (goal,) if goal else "거부: " + why))
cv2.imshow("ROOM_B 기울임", frames["ROOM_B 기울임"])
```

```text
qr.png (샘플)      → 읽음 'MentorPi'         거부: 형식 아님 ('MentorPi')
DELIVER:ROOM_A   → 읽음 'DELIVER:ROOM_A'   목표 (-1.0, 0.7, 0.0)
DELIVER:ROOM_B   → 읽음 'DELIVER:ROOM_B'   목표 (1.2, -0.7, 0.0)
DELIVER:KITCHEN  → 읽음 'DELIVER:KITCHEN'  거부: 모르는 목적지 ('KITCHEN')
ROOM_B 기울임       → 읽음 'DELIVER:ROOM_B'   목표 (1.2, -0.7, 0.0)
빈 화면             → 읽음 ''                 거부: QR 없음
```

- `qr.png`는 잘 읽히지만 `DELIVER:` 접두어가 없어 **거절**됩니다. 인식 성공과 명령 유효성은 별개입니다.
- `KITCHEN`은 형식은 맞지만 조회표에 없어 거절됩니다. `DOCK`도 배송 목적지로는 받지 않습니다.
- 원근으로 기울어진 지시서도 읽힙니다. 이미지 탭에서 확인하고, 기울기를 더 키워 한계를 찾아보세요.

### 실습 2. 배송 요청 서비스와 상태 기계

`delivery_bot`이 `/delivery/request`·`/delivery/confirm` 서비스를 제공하고, `front_desk` 노드가 1 s에 요청, 4 s에 중복 요청, 도착 2 s 뒤 수령 확인을 보냅니다. 메카넘 섀시는 방향을 유지한 채 목표 쪽으로 평행 이동합니다(M13 실습과 같은 방식). 실물에서는 `front_desk` 대신 `ros2 service call`을 씁니다.

```python run
import math, cv2, rclpy
from rclpy.node import Node
from std_msgs.msg import String
from std_srvs.srv import Trigger
from geometry_msgs.msg import Twist
from nav_msgs.msg import Odometry
import mentorpi_sim as sim                                        # 시뮬 전용
sim.setup(world="room", duration=28.0, start=(-0.8, -1.4, 0.0))  # 시뮬 전용

DOCK = (-0.8, -1.4)                                               # 출발 스테이션 = odom 원점 (yaw 0)
DEST = {"ROOM_A": (-1.0, 0.7), "ROOM_B": (1.2, -0.7)}
V, TOL, WAIT = 0.2, 0.05, 6.0                                     # 속도 상한, 도착 허용오차, 수령 대기 [s]

def camera():                                                     # 시뮬 전용: 스테이션 카메라에 비친 지시서
    q = cv2.resize(cv2.QRCodeEncoder.create().encode("DELIVER:ROOM_A"), None, fx=8, fy=8, interpolation=cv2.INTER_NEAREST)
    return cv2.copyMakeBorder(q, 32, 32, 32, 32, cv2.BORDER_CONSTANT, value=255)

class DeliveryBot(Node):
    def __init__(self):
        super().__init__('delivery_bot')
        self.cmd = self.create_publisher(Twist, '/controller/cmd_vel', 10)
        self.status = self.create_publisher(String, '/delivery/status', 10)   # 상태 방송 (UI·기록용)
        self.create_subscription(Odometry, '/odom', self.on_odom, 10)
        self.create_service(Trigger, '/delivery/request', self.on_request)
        self.create_service(Trigger, '/delivery/confirm', self.on_confirm)
        self.state, self.goal, self.pos, self.t_state = 'IDLE', None, None, 0.0
        self.create_timer(0.05, self.loop)
    def now(self): return self.get_clock().now().nanoseconds * 1e-9
    def go(self, st, goal=None, why=''):
        self.get_logger().info('%-8s -> %-8s %s' % (self.state, st, why))
        self.state, self.goal, self.t_state = st, goal, self.now()
        self.status.publish(String(data=st))
    def on_odom(self, m):
        self.pos = (DOCK[0] + m.pose.pose.position.x, DOCK[1] + m.pose.pose.position.y)
    def on_request(self, req, res):
        text = cv2.QRCodeDetector().detectAndDecode(camera())[0]  # 실물: 카메라 콜백이 읽어 둔 최신 QR
        name = text.partition(':')[2] if text.startswith('DELIVER:') else ''
        res.success = self.state == 'IDLE' and name in DEST
        res.message = ('accepted ' + name) if res.success else 'rejected (state=%s)' % self.state
        if res.success: self.go('DELIVER', DEST[name], name)
        return res
    def on_confirm(self, req, res):
        res.success = self.state == 'ARRIVED'
        if res.success: self.go('RETURN', DOCK, 'received')
        return res
    def loop(self):
        t = Twist()
        if self.state == 'ARRIVED' and self.now() - self.t_state > WAIT:
            self.go('RETURN', DOCK, 'no receiver (timeout)')
        if self.goal and self.pos and self.state in ('DELIVER', 'RETURN'):
            ex, ey = self.goal[0] - self.pos[0], self.goal[1] - self.pos[1]
            d = math.hypot(ex, ey)
            if d < TOL:
                self.go('ARRIVED' if self.state == 'DELIVER' else 'IDLE', None, 'at %.1f s' % self.now())
            else:
                s = min(V, 1.2 * d) / d                           # 메카넘: 방향 유지, 목표 쪽으로 평행 이동
                t.linear.x, t.linear.y = ex * s, ey * s
        self.cmd.publish(t)

class Desk(Node):                                                 # 요청자·수령자 역할 (실물: ros2 service call)
    def __init__(self):
        super().__init__('front_desk')
        self.req = self.create_client(Trigger, '/delivery/request')
        self.ok = self.create_client(Trigger, '/delivery/confirm')
        self.create_subscription(String, '/delivery/status', self.on_status, 10)
        self.plan = {1.0: self.req, 4.0: self.req}                # 1 s 요청, 4 s 중복 요청(거절돼야 함)
        self.create_timer(0.5, self.tick)
    def now(self): return self.get_clock().now().nanoseconds * 1e-9
    def tick(self):
        for t in [t for t in self.plan if self.now() >= t]:
            cli = self.plan.pop(t)
            cli.call_async(Trigger.Request()).add_done_callback(
                lambda f, c=cli: self.get_logger().info('%s -> %s' % (c.srv_name, f.result().message or f.result().success)))
    def on_status(self, msg):
        if msg.data == 'ARRIVED':
            self.plan[self.now() + 2.0] = self.ok                 # 물건을 꺼내는 데 2 s 뒤 수령 확인

rclpy.init()
bot, desk = DeliveryBot(), Desk()
try:
    rclpy.spin(bot)
except KeyboardInterrupt:
    pass
finally:
    bot.cmd.publish(Twist()); bot.destroy_node(); desk.destroy_node()
    if rclpy.ok(): rclpy.shutdown()
```

```text
[INFO] [1.000] [delivery_bot]: IDLE     -> DELIVER  ROOM_A   (대략)
[INFO] [1.000] [front_desk]: /delivery/request -> accepted ROOM_A
[INFO] [4.000] [front_desk]: /delivery/request -> rejected (state=DELIVER)
[INFO] [11.800] [delivery_bot]: DELIVER  -> ARRIVED  at 11.8 s
[INFO] [14.000] [delivery_bot]: ARRIVED  -> RETURN   received
[INFO] [14.000] [front_desk]: /delivery/confirm -> True
[INFO] [24.560] [delivery_bot]: RETURN   -> IDLE     at 24.6 s
[sim] 시뮬레이션 시간 28.0 s 종료 (sim.setup(duration=...)으로 변경)
```

- 서비스는 **즉시** 응답하고(1.000 s), 주행은 타이머 루프가 이어서 합니다. 4 s의 중복 요청은 상태가 IDLE이 아니라서 거절됩니다(F3).
- 도착(11.8 s) → 수령 확인(14.0 s) → 복귀(24.6 s). **2D 재생**에서 로봇이 방향을 바꾸지 않고 옆·뒤로 움직이는 것을 확인하세요.
- `Desk`의 수령 확인 줄(`self.plan[...] = self.ok`)을 지우면 `WAIT` 6 s 뒤 `no receiver (timeout)`으로 복귀합니다.

### 실습 3. 막힘 감지 · 재시도 · 재계획

같은 배송에서 통로에 카트(상자)가 놓여 있습니다. 진행 방향 ±25° 안에 0.35 m보다 가까운 것이 있으면 멈추고, 2 s 동안 계속 막히면 다음 시도로 넘어갑니다. 1·2회째는 같은 직선 경로(사람이라면 비켜 줄 수도 있으므로), 3회째는 서쪽으로 돌아가는 2순위 경로로 **재계획**합니다.

```python run
import math, rclpy
import numpy as np
from rclpy.node import Node
from geometry_msgs.msg import Twist
from sensor_msgs.msg import LaserScan
from nav_msgs.msg import Odometry
import mentorpi_sim as sim                                        # 시뮬 전용
sim.setup(world="room", duration=30.0, start=(-0.8, -1.4, 0.0))  # 시뮬 전용
sim.add_box(-0.8, -0.3, 0.5, 0.2)                                 # 시뮬 전용: 복도를 막은 카트

DOCK = (-0.8, -1.4)
ROUTES = [[(-1.0, 0.7)],                                          # 1순위: 직선 경로
          [(-1.5, -0.55), (-1.5, 0.3), (-1.0, 0.7)]]              # 2순위: 서쪽으로 돌아가는 경로
V, TOL, STOP_D, BLOCK_T, MAX_TRY = 0.2, 0.05, 0.35, 2.0, 3

class RecoveringDelivery(Node):
    def __init__(self):
        super().__init__('delivery_recovery')
        self.cmd = self.create_publisher(Twist, '/controller/cmd_vel', 10)
        self.create_subscription(Odometry, '/odom', self.on_odom, 10)
        self.create_subscription(LaserScan, '/scan', self.on_scan, 10)
        self.pos, self.scan, self.attempt = None, None, 0
        self.new_attempt('start')
        self.create_timer(0.05, self.loop)
    def now(self): return self.get_clock().now().nanoseconds * 1e-9
    def on_odom(self, m): self.pos = (DOCK[0] + m.pose.pose.position.x, DOCK[1] + m.pose.pose.position.y)
    def on_scan(self, s): self.scan = s
    def new_attempt(self, why):
        self.attempt, self.blocked_since = self.attempt + 1, None
        if self.attempt > MAX_TRY:
            self.get_logger().error('ABORT after %d tries -> RETURN to dock' % MAX_TRY)
            self.route, self.i = [DOCK], 0; return
        k = 0 if self.attempt < 3 else 1                           # 2회까지 같은 경로(비켜 주길 기다림), 3회째 재계획
        self.route, self.i = ROUTES[k], 0
        self.get_logger().info('attempt %d (%s): route %d %s' % (self.attempt, why, k + 1, self.route))
    def front_clear(self, vx, vy):                                # 진행 방향 ±25° 안에 STOP_D보다 가까운 것?
        r = np.array(self.scan.ranges); a = self.scan.angle_min + np.arange(len(r)) * self.scan.angle_increment
        diff = np.abs(np.arctan2(np.sin(a - math.atan2(vy, vx)), np.cos(a - math.atan2(vy, vx))))
        return not np.any((diff < math.radians(25)) & (r < STOP_D))
    def loop(self):
        if self.pos is None or self.scan is None: return
        t = Twist()
        gx, gy = self.route[self.i]
        ex, ey = gx - self.pos[0], gy - self.pos[1]; d = math.hypot(ex, ey)
        if d < TOL:
            self.i += 1
            if self.i == len(self.route):
                self.get_logger().info('ARRIVED (%s) at %.1f s, attempts %d' % (
                    'dock' if self.route == [DOCK] else 'ROOM_A', self.now(), self.attempt))
                self.cmd.publish(t); rclpy.shutdown(); return
        else:
            s = min(V, 1.2 * d) / d
            if self.front_clear(ex, ey):
                t.linear.x, t.linear.y, self.blocked_since = ex * s, ey * s, None
            elif self.blocked_since is None:
                self.blocked_since = self.now()
                self.get_logger().warn('blocked at (%.2f, %.2f) - stop' % self.pos)
            elif self.now() - self.blocked_since > BLOCK_T:
                self.new_attempt('blocked %.0f s' % BLOCK_T)
        self.cmd.publish(t)

rclpy.init()
node = RecoveringDelivery()
try:
    rclpy.spin(node)
except KeyboardInterrupt:
    pass
finally:
    if rclpy.ok(): node.cmd.publish(Twist())
    node.destroy_node()
    if rclpy.ok(): rclpy.shutdown()
```

```text
[INFO] [0.000] [delivery_recovery]: attempt 1 (start): route 1 [(-1.0, 0.7)]   (대략)
[WARN] [3.660] [delivery_recovery]: blocked at (-0.87, -0.70) - stop
[INFO] [5.700] [delivery_recovery]: attempt 2 (blocked 2 s): route 1 [(-1.0, 0.7)]
[WARN] [5.760] [delivery_recovery]: blocked at (-0.87, -0.68) - stop
[INFO] [7.800] [delivery_recovery]: attempt 3 (blocked 2 s): route 2 [(-1.5, -0.55), (-1.5, 0.3), (-1.0, 0.7)]
[INFO] [19.500] [delivery_recovery]: ARRIVED (ROOM_A) at 19.5 s, attempts 3
```

- 3.7 s에 카트 앞 0.35 m에서 멈추고(충돌 0), 2 s 간격으로 시도 2, 3이 이어집니다. 시도 3에서 경로를 바꿔 19.5 s에 도착합니다.
- `MAX_TRY`를 2로 줄이면 재계획 전에 `ABORT after 2 tries -> RETURN to dock`이 찍히고 11.7 s에 도크로 돌아옵니다. 반대로 `sim.add_box(-1.5, -0.1, 0.4, 0.2)`로 2순위 경로까지 막으면, 도크로 **곧장** 돌아가는 복귀 경로가 원기둥에 막혀 멈춥니다 — 복귀도 온 길을 되짚어야 한다는 뜻입니다(과제 1).
- 실물에서는 이 "막힘 감지"를 Nav2 컨트롤러와 `progress_checker`(10 s 동안 0.5 m 미만 이동이면 실패)가 대신하고, 우리는 **결과(FAILED)를 받아** 재시도·재계획만 결정합니다.

@btn[~/sim/index.html] 2D 시뮬레이터 열기

2D 시뮬레이터의 **내비게이션** 모드에서 room 월드의 같은 두 점(도크 → ROOM_A)을 클릭하고, 주행 중 경로 앞에 장애물을 두어 A* 전역 경로가 다시 계산되는 모습(Nav2의 재계획)과 위 실습 3의 임무 수준 재계획을 비교합니다.

@btn[~/sim3d/index.html] 3D 시뮬레이터 열기

## 실습 (실물)

:::safety
- 배송 경로 전체를 먼저 걸어 보며 계단·문턱·케이블·유리문을 확인합니다. 첫 배송은 한 사람이 로봇을 따라다닙니다.
- 적재물은 가볍고 낮은 것만 싣고 테이프로 고정합니다. **액체·유리·날카로운 물건 금지.** 무거운 물건은 라이다·카메라를 가리지 않게 합니다.
- 수령자는 로봇이 **완전히 멈추고 파랑 LED가 켜진 뒤** 물건을 꺼냅니다. 꺼내는 동안 손을 바퀴 근처에 두지 않습니다.
- 멈추는 방법: 스크립트 Ctrl+C(`cancelTask`), `ros2 topic pub --once /controller/cmd_vel geometry_msgs/msg/Twist "{}"`, 전원 스위치. 속도 상한(0.2 m/s, 0.5 rad/s)은 풀지 않습니다.
- 고장 주입 시험에서 로봇을 들어 옮길 때는 먼저 Ctrl+C로 노드를 멈춥니다.
:::

### 1) 카메라와 QR 확인 (M2)

A02와 같은 절차로 원본 예제를 실행해 지시서를 읽는 범위를 잽니다.

```bash
~/.stop_ros.sh
ros2 launch peripherals depth_camera.launch.py
# 새 터미널
python3 /home/ubuntu/ros2_ws/src/example/example/qrcode/qrcode_detecter.py
```

지시서 인쇄용 QR은 원본 `qrcode_creater.py`의 문자열을 `DELIVER:ROOM_A`처럼 바꿔 만들거나, Playground 실습 1의 `make_qr()` 결과를 `cv2.imwrite()`로 저장해 인쇄합니다.

### 2) 내비게이션과 조회표 (M1)

```bash
# 로봇 (Ctrl+C로 앞의 launch 종료 후) — 카메라도 함께 켜짐(robot.launch.py)
ros2 launch navigation navigation.launch.py map:=map_01

# 가상머신
ros2 launch navigation rviz_navigation.launch.py
ros2 topic echo /clicked_point          # Publish Point로 도크·목적지 좌표 수집
```

로봇을 도크에 두고 **2D Pose Estimate**로 초기 위치를 지정합니다.

### 3) 배송 노드

```python robot
#!/usr/bin/env python3
# delivery_bot.py — navigation.launch.py 실행 + 도크에서 초기 위치 지정 후
import math
import cv2
import rclpy
from rclpy.time import Time
from cv_bridge import CvBridge
from sensor_msgs.msg import Image
from std_msgs.msg import String
from std_srvs.srv import Trigger
from geometry_msgs.msg import PoseStamped
from nav2_simple_commander.robot_navigator import BasicNavigator, TaskResult
from tf2_ros import Buffer, TransformListener, TransformException
from tf_transformations import quaternion_from_euler
from ros_robot_controller_msgs.msg import RGBState, RGBStates, BuzzerState, ButtonState

DOCK = (0.0, 0.0, 0.0)                                         # map 좌표 (x, y, yaw) — /clicked_point로 측정
DEST = {'ROOM_A': [(2.0, 1.0, 1.57), (2.0, 0.6, 1.57)],       # 이름 → [주 접근점, 대체 접근점]
        'ROOM_B': [(3.5, -0.5, 0.0), (3.2, -0.8, 0.0)]}
NAV_TIMEOUT, WAIT_T, MAX_TRY, ARRIVE_TOL = 90.0, 60.0, 3, 0.30
GREEN, BLUE, YELLOW = (0, 255, 0), (0, 0, 255), (255, 255, 0)

class DeliveryBot(BasicNavigator):
    def __init__(self):
        super().__init__('delivery_bot')
        self.bridge, self.det = CvBridge(), cv2.QRCodeDetector()
        self.qr, self.qr_t, self.frame_t = '', -1e9, -1e9
        self.create_subscription(Image, '/ascamera/camera_publisher/rgb0/image', self.on_image, 1)
        self.create_subscription(ButtonState, '/ros_robot_controller/button', self.on_button, 10)
        self.create_service(Trigger, '/delivery/request', self.on_request)
        self.create_service(Trigger, '/delivery/confirm', self.on_confirm)
        self.status = self.create_publisher(String, '/delivery/status', 10)
        self.rgb = self.create_publisher(RGBStates, '/ros_robot_controller/set_rgb', 1)
        self.buzzer = self.create_publisher(BuzzerState, '/ros_robot_controller/set_buzzer', 1)
        self.tf_buffer = Buffer()
        self.tf_listener = TransformListener(self.tf_buffer, self)
        self.state, self.dest, self.target, self.attempt = 'IDLE', None, None, 0
        self.t_state = self.t_nav = self.now()
        self.confirmed = False
    def now(self):
        return self.get_clock().now().nanoseconds * 1e-9
    def go(self, state, why=''):                                 # 상태 전이 + 방송 + 표시
        self.get_logger().info('%s -> %s %s' % (self.state, state, why))
        self.state, self.t_state = state, self.now()
        self.status.publish(String(data=state))
        color, beep = {'IDLE': (GREEN, None), 'ARRIVED': (BLUE, (1900, 0.1, 2)),
                       'SAFE_STOP': (YELLOW, (1000, 0.5, 3))}.get(state, (GREEN, None))
        self.rgb.publish(RGBStates(states=[RGBState(index=i, red=color[0], green=color[1], blue=color[2]) for i in (1, 2)]))
        if beep:
            self.buzzer.publish(BuzzerState(freq=beep[0], on_time=beep[1], off_time=beep[1], repeat=beep[2]))
    # ---- 콜백: 상태만 바꾸고 바로 돌아온다
    def on_image(self, msg):
        if self.state != 'IDLE' or self.now() - self.frame_t < 0.5:  # 도크에서만, 2 Hz
            return
        self.frame_t = self.now()
        text = self.det.detectAndDecode(self.bridge.imgmsg_to_cv2(msg, 'bgr8'))[0]
        if text:
            self.qr, self.qr_t = text, self.now()
    def on_request(self, req, res):
        fresh = self.now() - self.qr_t < 2.0
        name = self.qr.partition(':')[2] if fresh and self.qr.startswith('DELIVER:') else ''
        res.success = self.state == 'IDLE' and name in DEST
        res.message = ('accepted ' + name) if res.success else 'rejected (state=%s, qr=%r)' % (self.state, self.qr if fresh else '')
        if res.success:
            self.dest, self.attempt, self.state = name, 0, 'START'  # goToPose는 주 루프에서
        return res
    def on_confirm(self, req, res):
        res.success = self.state == 'ARRIVED'
        self.confirmed = self.confirmed or res.success
        return res
    def on_button(self, msg):
        if self.state == 'ARRIVED' and msg.id == 1 and msg.state in (1, 5):   # 누름 또는 클릭
            self.confirmed = True
    # ---- 주 루프에서 부르는 함수들
    def drive(self, pose):
        p = PoseStamped()
        p.header.frame_id, p.header.stamp = 'map', self.get_clock().now().to_msg()
        p.pose.position.x, p.pose.position.y = pose[0], pose[1]
        q = quaternion_from_euler(0.0, 0.0, pose[2])
        p.pose.orientation.x, p.pose.orientation.y, p.pose.orientation.z, p.pose.orientation.w = q
        self.target, self.t_nav = pose, self.now()
        self.goToPose(p)
    def error_to_target(self):
        try:
            t = self.tf_buffer.lookup_transform('map', 'base_footprint', Time()).transform.translation
        except TransformException:
            return float('inf')
        return math.hypot(self.target[0] - t.x, self.target[1] - t.y)
    def recover(self, why):
        self.attempt += 1
        if self.attempt > MAX_TRY:
            if self.state == 'RETURN':
                self.go('SAFE_STOP', 'cannot reach dock')
                return
            self.attempt = 1
            self.go('RETURN', 'aborted: ' + why)
            self.drive(DOCK)
            return
        self.get_logger().warn('%s failed (%s) -> try %d' % (self.state, why, self.attempt))
        self.clearAllCostmaps()                                  # 지나간 사람의 흔적 지우기
        if self.state == 'RETURN':
            self.drive(DOCK)
        else:
            self.drive(DEST[self.dest][0 if self.attempt < 3 else 1])   # 3회째: 대체 접근점으로 재계획
    def step(self):
        if self.state == 'START':
            self.attempt = 1
            self.go('DELIVER', self.dest)
            self.drive(DEST[self.dest][0])
        elif self.state in ('DELIVER', 'RETURN'):
            if not self.isTaskComplete():
                if self.now() - self.t_nav > NAV_TIMEOUT:
                    self.cancelTask()                            # 다음 루프에서 CANCELED로 처리
                return
            err = self.error_to_target()
            if self.getResult() != TaskResult.SUCCEEDED or err > ARRIVE_TOL:
                self.recover('result=%s err=%.2f m' % (self.getResult(), err))
            elif self.state == 'DELIVER':
                self.confirmed = False
                self.go('ARRIVED', '%s err %.2f m' % (self.dest, err))
            else:
                self.go('IDLE', 'docked')
        elif self.state == 'ARRIVED' and (self.confirmed or self.now() - self.t_state > WAIT_T):
            self.attempt = 1
            self.go('RETURN', 'delivered' if self.confirmed else 'no receiver')
            self.drive(DOCK)

def main():
    rclpy.init()
    bot = DeliveryBot()
    bot.waitUntilNav2Active()
    bot.go('IDLE', 'ready')
    try:
        while rclpy.ok() and bot.state != 'SAFE_STOP':
            rclpy.spin_once(bot, timeout_sec=0.05)
            bot.step()
    except KeyboardInterrupt:
        pass
    finally:
        bot.cancelTask()
        bot.go('SAFE_STOP', 'shutdown')
        bot.destroy_node()
        rclpy.shutdown()

if __name__ == '__main__':
    main()
```

```bash
python3 ~/delivery_bot.py
# 새 터미널: 스테이션 카메라에 DELIVER:ROOM_A 지시서를 보여 준 채로
ros2 service call /delivery/request std_srvs/srv/Trigger {}
ros2 topic echo /delivery/status
# 도착 후 (보드 버튼 대신)
ros2 service call /delivery/confirm std_srvs/srv/Trigger {}
```

- 콜백(`on_image`, `on_request`, `on_confirm`, `on_button`)은 변수만 바꿉니다. `goToPose`·`cancelTask`·`clearAllCostmaps`처럼 안에서 spin하는 함수는 모두 `step()`에서 부릅니다.
- 도착 판정은 `SUCCEEDED`만 믿지 않고 TF로 실제 위치를 한 번 더 봅니다. AMCL이 튄 경우(로봇을 들어 옮김 등) 여기서 걸러져 재시도로 갑니다.
- `ButtonState`의 `state`는 원본 `ros_robot_controller_node.py` 정의를 따릅니다(1 누름, 5 클릭, 0 짧게 누른 뒤 뗌).

### 4) 고장 주입 시험 (M4)

| 시험 | 방법 | 기대 결과 |
|---|---|---|
| T1 잠깐 막힘 | 주행 중 사람이 5 s 경로에 섬 | 컨트롤러 회피 또는 2회째 재시도로 성공 |
| T2 문 앞 막힘 | 주 접근점에 상자 | 3회째 대체 접근점 도착, 로그에 `try 3` |
| T3 완전 차단 | 목적지로 가는 통로 전체를 막음 | `aborted` 후 도크 복귀, IDLE |
| T4 미수령 | 도착 후 아무도 확인 안 함 | 60 s 뒤 `no receiver`로 복귀 |
| T5 잘못된 지시서 | `MentorPi` QR, 모르는 방 이름 | `rejected`, 로봇 움직이지 않음 |

끝나면 모든 터미널 Ctrl+C 후 호스트에서 `sudo systemctl restart start_node.service`로 앱 서비스를 되돌립니다.

## 평가 기준

| 영역 | 항목 | 배점 | 만점 기준 |
|---|---|---|---|
| 설계 | 요구사항·안전 한계 표 | 10 | 모든 항목이 측정 가능한 값 |
| 설계 | 인터페이스 선택 근거·상태 기계 그림 | 10 | 토픽/서비스/액션 선택 이유, 전이 조건이 코드와 일치 |
| 구현 | QR 접수 (M2) | 15 | 형식 검증, 거절 3종, 인식 범위 측정 |
| 구현 | 배송·도착 확인·복귀 (M3) | 20 | 이중 도착 확인, 버튼·서비스·시간 초과 |
| 구현 | 실패 복구 (M4) | 20 | T1~T3 모두 설계대로, 무한 재시도 없음 |
| 시험 | 연속 10건 결과표 (M5) | 15 | 건별 시간·시도·결과, 실패 원인 분석 |
| 안전 | 적재·정지 절차 | 10 | 적재물 고정, 정지 수단 시연, 속도 한계 유지 |
| **합계** | | **100** | |

감점: 서비스 콜백 안에서 `goToPose` 호출(멈춤 위험) −5, 좌표를 QR에 직접 넣는 설계 −5, 속도 상한 완화 −10.

## 확장 과제

- **액션 서버**: `delivery_interfaces` 패키지(`ament_cmake`, `rosidl_generate_interfaces`)에 `Deliver.action`을 정의하고 피드백(`state`, `distance_remaining`)과 취소를 지원합니다. 취소되면 즉시 RETURN으로 갑니다.
- **배송 대기열**: 요청을 거절하는 대신 큐에 넣고, 복귀하면 다음 건을 처리합니다. 지시서마다 `DELIVER:ROOM_A#17`처럼 건 번호를 붙여 중복 접수를 막습니다.
- **수령자 인증**: 도착지에서 수령자가 보여 준 두 번째 QR(`RECEIVE:17`)을 카메라로 읽어야만 수령 확인으로 인정합니다.
- **경로 경유점**: 좁은 복도는 `goThroughPoses`로 안전한 경유점을 지나게 합니다(M13).
- **배터리 인식**: `/ros_robot_controller/battery`가 기준 아래면 새 요청을 거절하고 도크에 머뭅니다.

## 자주 나는 오류와 해결

| 증상 | 원인 | 해결 |
|---|---|---|
| 요청하면 서비스 호출이 끝나지 않음 | 서비스 콜백 안에서 `goToPose()` 호출 → 실행기 중첩 | 콜백은 상태만 바꾸고 주 루프에서 주행 시작 |
| 항상 `rejected (qr='')` | 카메라 토픽 없음, 2 s 넘은 오래된 QR, 너무 멀거나 반사 | `ros2 topic hz /ascamera/camera_publisher/rgb0/image`, 30~60 cm, 무광 인쇄 |
| QR은 보이는데 `detectAndDecode`가 빈 문자열 | 여백 부족, 모듈이 너무 작음 | 여백 4모듈, 한 모듈이 영상에서 3~4픽셀 이상 (A02) |
| `SUCCEEDED`인데 ARRIVED가 안 됨 | TF 오차 > 0.3 m (AMCL 튐) 또는 `base_footprint` TF 없음 | RViz에서 스캔·지도 정렬 확인, `ros2 run tf2_ros tf2_echo map base_footprint` |
| 같은 문 앞에서 계속 실패 | 주·대체 접근점이 모두 팽창 영역 안 | 접근점을 벽에서 0.4 m 이상, 코스트맵 확인(M12) |
| 버튼을 눌러도 확인이 안 됨 | 다른 버튼 id, 앱 서비스의 버튼 점유 | `ros2 topic echo /ros_robot_controller/button`으로 id·state 확인 |
| 복귀 후 다음 요청이 거절됨 | 상태가 IDLE로 안 돌아옴(도크 도착 판정 실패) | 도크 좌표·방향 재측정, 로그의 err 값 확인 |
| `waitUntilNav2Active`에서 멈춤 | 초기 위치 미지정 | 도크에서 2D Pose Estimate 후 실행 |

## 과제

:::task 기본 과제
1. Playground 실습 3에서 `STOP_D`(0.25/0.35/0.5 m)와 `BLOCK_T`(1/2/4 s)를 바꿔 도착 시간과 충돌 여부를 표로 정리합니다. 이어서 지나온 지점을 기록해 두었다가 ABORT 때 **역순으로 되짚어** 도크로 가도록 고치고, 2순위 경로까지 막힌 경우에도 복귀하는 것을 보입니다.
2. 실물에서 T1~T5를 수행하고 결과표를 제출합니다.
:::

:::task 심화 과제
3. 확장 과제의 **액션 서버**를 구현하고, `ros2 action send_goal /deliver delivery_interfaces/action/Deliver "{destination: ROOM_A}" --feedback`으로 진행 피드백을 받는 것을 시연합니다.
4. 두 목적지를 한 번에 도는 **다중 배송**(`DELIVER:ROOM_A,ROOM_B`)을 설계합니다. 한 곳이 실패해도 다른 곳은 배송하고, 결과 보고에 건별 성공·실패를 남깁니다.
:::

## 참고자료

- 원본 교재: *14. ROS+OpenCV Lesson* — Lesson 3 Generate & Recognize QR Code, *13. Navigation Lesson* — Lesson 2 AMCL Adaptive Monte Carlo Positioning, Lesson 4 Point-to-Point and Multi-Point Navigation and Obstacle Avoidance, *6. Raspberry Pi 5 & Expansion Board* — Lesson 3 Button Control, Lesson 4 Buzzer Control
- 원본 소스: `src/example/example/qrcode/qrcode_detecter.py`, `qrcode_creater.py`, `src/navigation/launch/navigation.launch.py`, `src/slam/launch/include/robot.launch.py`(깊이 카메라 포함), `src/navigation/config/nav2_controller_dwb.yaml`(`progress_checker`, `general_goal_checker`), `src/driver/ros_robot_controller/ros_robot_controller/ros_robot_controller_node.py`(`ButtonState` 상태 값)
- Nav2 Simple Commander API (`goToPose`, `cancelTask`, `clearAllCostmaps`): https://docs.nav2.org/commander_api/index.html
- ROS 2 인터페이스 만들기 (srv·action): https://docs.ros.org/en/humble/Tutorials/Beginner-Client-Libraries/Custom-ROS2-Interfaces.html
- ROS 2 액션 서버 작성 (Python): https://docs.ros.org/en/humble/Tutorials/Intermediate/Writing-an-Action-Server-Client/Py.html
- OpenCV `QRCodeDetector`: https://docs.opencv.org/4.x/de/dc3/classcv_1_1QRCodeDetector.html
