---
id: p03
track: project
title: 미니 시티 자율주행 챌린지
duration: 300
level: 고급
requires: [a03, a09, a10, a11]
tools: [sim3d, sim, playground]
---

## 학습 목표

- 차선 유지·신호등·표지판·교차로 회전·주차를 **하나의 유한 상태 기계(FSM)**로 통합하고, 인식 계층(사건 생성)과 결정 계층(전이표)을 분리해 설계합니다.
- 원본 `self_driving.py`의 알려진 약점(차선 상실 시 마지막 명령 유지, 같은 값인 정상·서행 속도, 쓰이지 않는 우회전 표지, 개루프 주차, 장애물 무시)을 찾아 고치고, 고친 효과를 데이터로 보입니다.
- 챌린지 규칙과 채점표를 이해하고, 시간·감점을 고려해 속도·디바운싱·타임아웃 파라미터를 고릅니다.
- 브라우저 시뮬레이션으로 FSM·차선 오차·도시 순환 주행을 먼저 검증한 뒤, 실물 MentorPi에서 팀 노드를 실행하고 기록(rosbag)으로 채점을 재현합니다.
- 팀 안에서 인식·제어·안전·심판 역할을 나눠 인터페이스(토픽·사건 이름)를 먼저 합의하는 방식으로 협업합니다.

## 프로젝트 개요와 요구사항

A09~A11에서 차선 유지, YOLOv5 표지판·신호등 인식, 회전 판단과 주차를 **따로따로** 익혔습니다. 이 프로젝트는 그것들을 한 대의 로봇 안에서 **동시에, 충돌 없이** 돌게 만드는 통합 과제입니다. 기능 하나하나보다 어려운 것은 "언제 무엇을 우선하는가"입니다. 횡단보도 앞에서 빨간불을 봤는데 차선이 흐려졌다면? 주차 표지를 봤는데 앞에 상자가 놓여 있다면? 이런 질문에 **표 한 장으로 답할 수 있는 설계**가 이 장의 목표입니다.

진행은 4~5차시(약 300분) 팀 프로젝트를 가정합니다.

| 차시 | 활동 | 산출물 |
|---|---|---|
| 1 | 원본 통합 주행으로 기준선 측정, 요구사항·역할 합의 | 기준선 채점표, 사건 이름 목록 |
| 2 | FSM 전이표 설계, 시뮬 프로토타입(실습 1·2) | 전이표, 사건 정의표 |
| 3 | 도시 순환 시뮬(실습 3), 실물 차선·신호 통합 | 시뮬 로그, 실물 주행 영상 |
| 4 | 교차로·주차·안전 기능, 파라미터 튜닝 | 튜닝 기록표 |
| 5 | 챌린지 본선 2회 + 발표·회고 | 채점표, rosbag, 발표 자료 |

역할은 네 명 기준으로 나눕니다. 인원이 적으면 한 사람이 두 역할을 맡습니다.

| 역할 | 책임 | 주로 만지는 코드 |
|---|---|---|
| 인식 | 차선 오차, YOLOv5 검출 → **사건**으로 변환(디바운싱) | `on_image`, `on_objects` |
| 결정·제어 | 전이표, 상태별 속도·조향, 주차 동작 | `TABLE`, `drive()` |
| 안전·통합 | 라이다 감시, 타임아웃, 워치독, launch·실행 절차 | `on_scan`, `watchdog()` |
| 심판·기록 | 채점 규칙 적용, 스톱워치, rosbag, 튜닝 기록 | 채점표, `ros2 bag` |

### 기능 요구사항

| ID | 요구사항 | 확인 방법 |
|---|---|---|
| F1 | 노란 차선을 따라 코스 1바퀴를 차로 이탈 없이 주행 | 영상, 이탈 횟수 |
| F2 | 빨간불이면 정지선 앞에서 정지, 초록불이 되면 3 s 안에 출발 | 스톱워치, `~/state` 로그 |
| F3 | 횡단보도 구간에서 서행(0.07 m/s 이하) | `/controller/cmd_vel` 기록 |
| F4 | 교차로에서 **표지(`right`)에 따라** 회전 또는 직진 | 표지를 바꿔 2회 시행 |
| F5 | 주차 표지를 보고 주차 칸에 완전히 들어가 정지 | 줄자로 중심·각도 측정 |
| F6 | 차선을 3프레임 연속 잃으면 정지하고, 다시 찾으면 재개 | 맵 일부를 종이로 가려 시험 |
| F7 | 전방 0.25 m 안에 물체가 있으면 어느 상태에서든 정지 | 상자를 앞에 놓아 시험 |
| F8 | 현재 상태를 `~/state`(String)로 발행하고 전이마다 로그 | `ros2 topic echo` |

### 안전 한계

| 항목 | 값 | 근거 |
|---|---|---|
| 최대 선속도 | 0.12 m/s (차선 유지), 서행 0.06 m/s | 원본 0.1 m/s, 강의 권장 한계 0.2 m/s |
| 최대 각속도 | 0.45 rad/s(회전 모드), PID 출력 ±0.3 rad/s | 원본 회전 −0.45, 권장 한계 0.5 |
| 전방 정지 거리 | 0.25 m (±20° 섹터) | 0.12 m/s에서 반응 0.15 s + 제동 ≈ 0.03 m, 차체 반경 0.13 m + 여유 |
| 차선 상실 | 3프레임(약 0.1 s) 연속 미검출 → 정지 | 원본은 명령을 안 보내 마지막 명령 유지 |
| 영상 워치독 | 0.5 s 동안 영상이 없으면 정지 | 카메라 노드 정지·지연 대비 |
| 상태 타임아웃 | STOP 20 s, LOST 3 s, HOLD 10 s → ABORT(정지) | 영원히 기다리는 상태 방지 |
| 비상 정지 | `~/set_running false`, `Ctrl+C`, 전원 스위치 | 심판이 언제든 호출 |

### 원본에서 고칠 약점

A09~A11에서 확인한 원본 `self_driving.py`의 문제를 요구사항과 연결해 둡니다. 발표 때 **고치기 전/후 데이터**를 함께 보여야 합니다.

| 원본 동작 | 문제 | 이 프로젝트의 해결 | 관련 요구 |
|---|---|---|---|
| `lane_x < 0`이면 Twist를 발행하지 않음 | 모터가 마지막 명령(예: 우회전 −0.45)을 유지하며 맵 밖으로 | LOST 상태: 3프레임 뒤 정지 명령 발행 | F6 |
| `normal_speed = slow_down_speed = 0.1` | "서행"해도 속도가 같음 | 상태별 속도표 LANE 0.12 / SLOW 0.06 | F3 |
| `turn_right`를 세우지만 주 루프에서 안 읽음 | 표지가 회전을 결정하지 않음 | 표지는 **기억(memo)**, 교차로 사건에서 소비 | F4 |
| 빨간불 판단이 감속 상태 안에서만, 면적 < 1000 | 횡단보도 인식이 늦으면 신호 무시 | SLOW 상태의 전이로 명시, red 1프레임·green 5프레임 | F2 |
| `PID(0.4, 0, 0.05)` + ±0.1 제한 | 사실상 뱅뱅 제어, 조향 떨림 | Kp 0.004, Kd 0.002, ±0.3 (A09 시뮬 결과) | F1 |
| `park_action()`이 시간 기반 개루프 | 배터리·바닥에 따라 주차 위치가 달라짐 | `/odom`으로 이동 거리를 재는 폐루프 | F5 |
| 라이다를 쓰지 않음 | 코스 위 장애물과 충돌 | HOLD 안전 상태 | F7 |

### 챌린지 규칙과 채점

@fig[p03-course] 예시 코스와 다섯 채점 구역. 실제 배치는 원본 맵의 표지·신호등 위치에 맞춥니다.

**규칙**

1. 로봇은 출발 게이트(①)에 차로 가운데로 놓고, 심판의 "출발" 신호와 함께 팀원이 노드를 시작합니다. 이후 로봇에 손을 대면 **개입**입니다.
2. 신호등은 심판이 손으로 바꿉니다. 로봇이 정지선에 다가오는 동안 빨간불로 두고, 정지한 뒤 3~8 s 사이 임의 시점에 초록으로 바꿉니다.
3. 교차로 표지(④)는 시행 전에 심판이 `right` 또는 `go` 중 하나로 정합니다(팀은 모름).
4. 코스 1바퀴 뒤 주차 칸(⑤)에 정지하면 완주입니다. 제한 시간은 **5분**이며, 넘기면 그 시점까지의 점수만 인정합니다.
5. 본선은 2회 시행하고 높은 점수를 씁니다. 시행 사이 파라미터 수정은 허용하되 5분 안에 끝냅니다.

**점수표 (100점)**

| 구역 | 항목 | 점수 |
|---|---|---|
| M1 | 1바퀴 완주(차로 유지) | 20 |
| M2 | 빨간불 정지선 앞 정지 10 + 초록불 3 s 안에 출발 5 | 15 |
| M3 | 횡단보도 서행(≤ 0.07 m/s, 로그 확인) | 10 |
| M4 | 교차로에서 표지대로 진행 | 10 |
| M5 | 주차 칸 완전 진입 15 + 중심 ±3 cm·각도 ±10° 이내 5 | 20 |
| M6 | 안전 시연: 심판이 차로에 상자를 놓으면 정지 → 치우면 재개 | 10 |
| T | 시간 점수 $= clamp(15 − (T − 90)/10, 0, 15)$ (T: 완주 시간 [s]) | 15 |

| 감점 | 값 |
|---|---|
| 차로 이탈(바퀴 두 개 이상이 선 밖) | −5 / 회 |
| 소품·벽 접촉 | −10 / 회 |
| 사람 개입(손대기·재배치) | −10 / 회, 3회면 실격 |
| 빨간불에 정지선 통과 | −15, M2 0점 |

시간 점수는 90 s 이내 완주면 만점이고, 240 s면 0점입니다. 속도를 올리면 T는 줄지만 이탈·접촉 감점 위험이 커지므로, **감점 한 번(−5)이 시간 50 s와 같다**는 점을 기억하고 파라미터를 고르세요.

## 시스템 설계

### 노드와 토픽

@fig[p03-graph] 팀 노드 `city_fsm` 하나가 모든 인식 결과를 받아 속도 명령을 단독으로 발행합니다.

| 토픽/서비스 | 타입 | 방향 | 용도 |
|---|---|---|---|
| `/ascamera/camera_publisher/rgb0/image` | `sensor_msgs/Image` | 구독 | 차선 검출(`LaneDetector('yellow')`) |
| `/yolov5_ros2/object_detect` | `interfaces/ObjectsInfo` | 구독 | `go`, `right`, `park`, `red`, `green`, `crosswalk` |
| `/scan_raw` | `sensor_msgs/LaserScan` | 구독 (sensor QoS) | 전방 장애물 감시 |
| `/odom` | `nav_msgs/Odometry` | 구독 | 주차 이동 거리(폐루프) |
| `/controller/cmd_vel` | `geometry_msgs/Twist` | 발행 | 유일한 속도 출력 |
| `~/state` | `std_msgs/String` | 발행 | 심판·디버깅용 상태 기록 |
| `~/set_running` | `std_srvs/SetBool` | 서비스 | 일시정지·재개(원본과 같은 이름) |

설계 원칙 네 가지를 먼저 합의합니다.

1. **단일 출력.** `/controller/cmd_vel`은 `city_fsm`만 발행합니다. 원본처럼 주차 스레드와 주 루프가 따로 발행하면, 어느 명령이 마지막인지에 따라 동작이 달라집니다.
2. **사건 계층.** 인식 코드는 YOLO 박스나 `lane_x`를 직접 FSM에 넘기지 않고, 디바운싱을 거친 **사건 이름**(`crosswalk`, `red`, `junction` …)만 넘깁니다. 인식 담당과 제어 담당이 사건 이름 목록만 맞추면 따로 개발할 수 있습니다.
3. **안전 우선.** `lane_lost`, `obstacle` 같은 안전 사건은 전이표보다 먼저 검사하고, 어느 상태에서든 LOST·HOLD로 보냅니다. 해제되면 **들어오기 전 상태로** 돌아갑니다.
4. **모든 대기에 시간 제한.** STOP·LOST·HOLD는 제한 시간을 넘기면 ABORT(정지)로 갑니다.

### 상태 기계

@fig[p03-fsm] 미니 시티 FSM. A11의 FSM에 교차로 분기(TURN_R/TURN_S), 안전 상태(LOST·HOLD), 시간 제한을 더했습니다.

| 현재 상태 | 사건 | 다음 상태 | 진입 시 동작 |
|---|---|---|---|
| LANE | `crosswalk` | SLOW | 서행 시작 시각 기록 |
| SLOW | `red` | STOP | 정지 명령 즉시 발행 |
| STOP | `green` | SLOW | |
| SLOW | `passed` | LANE | |
| LANE | `junction` | TURN_R 또는 TURN_S | 표지 기억(`memo`)을 읽고 **지움** |
| TURN_R / TURN_S | `straight` | LANE | PID 초기화 |
| LANE | `park_sign` | APPROACH | |
| APPROACH | `at_spot` | PARK | 주차 시작 위치(`/odom`) 기록 |
| PARK | `done` | DONE | 정지, 완주 시간 로그 |
| (모든 상태) | `lane_lost` / `obstacle` | LOST / HOLD | 이전 상태 저장, 정지 |
| LOST / HOLD | `lane_found` / `clear` | 이전 상태 | |
| STOP·LOST·HOLD | 시간 초과 | ABORT | 정지, 부저·로그 |

### 사건 정의

| 사건 | 원천 신호 | 확인 조건(디바운싱) | 비고 |
|---|---|---|---|
| `crosswalk` | YOLO `crosswalk` 박스 중심 y > 70 | 3프레임 연속 | 원본과 같은 문턱 |
| `red` | YOLO `red` | **1프레임** | 멈추는 쪽은 빠르게 |
| `green` | YOLO `green` | **5프레임** | 출발하는 쪽은 신중하게 |
| `passed` | SLOW 진입 후 경과 시간 | 0.4 m / 서행 속도 | 원본 `crosswalk_length` |
| `sign_right` | YOLO `right` | 5프레임 | 상태를 바꾸지 않고 기억만 |
| `junction` | `lane_x > 150` | 6프레임 연속 | 원본 `count_turn > 5` |
| `straight` | `lane_x ≤ 150` 그리고 회전 2 s 경과 | 1프레임 | 원본 시간 히스테리시스 |
| `park_sign` | `park` 보임 + 횡단보도 y > 135 | 5프레임 | |
| `at_spot` | 횡단보도 y > 180 | 5프레임 | 원본 15프레임을 서행으로 보완 |
| `lane_lost` | `lane_x < 0` | 3프레임 연속 | 원본에 없음 |
| `obstacle` | `/scan_raw` 전방 ±20° 최소 < 0.25 m | 2스캔 | 해제는 > 0.35 m(히스테리시스) |

빨간불과 초록불의 확인 프레임 수가 다른 것은 **오류의 비용이 비대칭**이기 때문입니다. 빨간불을 잘못 봐서 잠깐 서는 비용은 작지만, 초록불을 잘못 봐서 출발하는 비용은 큽니다.

## 단계별 구현

### M1. 기준선 측정

:::task M1 — 원본으로 한 바퀴
1. 원본 통합 주행(`self_driving.launch.py`)을 코스에서 2회 실행하고 채점표로 점수를 매깁니다.
2. 실패한 지점마다 "어떤 사건이 늦었나/빠졌나"를 적습니다. 예: "차선 상실 후 우회전 명령 유지로 맵 이탈".
3. `ros2 bag record /controller/cmd_vel /yolov5_ros2/object_detect`로 기록해 둡니다. M6에서 개선 전후 비교에 씁니다.
:::

:::check 체크포인트 M1
- [ ] 기준선 점수와 실패 원인 목록이 있다.
- [ ] 팀이 쓸 사건 이름 11개(위 표)를 합의했다.
:::

### M2. FSM 골격과 사건 로그

전이표를 코드의 **데이터**(딕셔너리)로 두고, 순수 Python으로 사건 타임라인을 흘려 검증합니다(실습 1). 체크포인트: 팀이 걱정하는 상황(예: 빨간불 정지 중 차선 상실)을 타임라인에 넣어도 기대한 상태가 나오고, 전이표의 빈 칸을 "무시"로 둬도 되는지 한 칸씩 검토했다.

### M3. 차선 오차와 상실 처리

원본의 ±0.1 뱅뱅 제어를 PD로 바꾸고, 차선이 없을 때의 동작을 정합니다(실습 2). 체크포인트: 맵 일부를 A4 용지로 덮으면 3프레임 안에 멈추고 치우면 다시 가며, 직선 20초 동안 조향 부호 반전 횟수를 원본과 비교했다.

### M4. 신호등·표지 사건

YOLOv5 콜백에서 사건을 만들고, FSM은 사건만 봅니다. 표지 `right`는 **상태를 바꾸지 않고 기억**만 하고, 교차로(`junction`) 사건이 올 때 읽고 지웁니다. 체크포인트: 바퀴를 띄운 채 신호등을 바꾸면 `~/state`가 STOP↔SLOW로 바뀌고, 표지를 치우면 교차로에서 TURN_S가 로그에 찍힌다.

### M5. 교차로 회전과 폐루프 주차

회전은 원본처럼 고정 각속도(−0.45 rad/s)로 돌되 `straight` 사건으로 끝냅니다. 주차는 메카넘 기준 **옆으로 0.38 m**(원본 값)를 시간 대신 `/odom` 이동 거리로 잽니다. 체크포인트: 실습 3의 시뮬 채점이 75/75이고, 실물 주차 3회 반복 편차가 2 cm 이하(원본 시간 기반과 비교)다.

### M6. 통합 리허설

:::task M6 — 본선 전 리허설
1. 본선 규칙 그대로 2회 시행하고 채점합니다. 심판 역할은 다른 팀이 맡으면 좋습니다.
2. 안전 시연(M6 구역): 주행 중 상자를 차로에 놓고 HOLD → 치우면 원래 상태로 복귀하는지 봅니다.
3. M1의 기록과 같은 형식으로 rosbag을 남겨 **개선 전후 표**를 만듭니다(서행 속도, 조향 반전 횟수, 주차 편차, 차선 상실 후 이동 거리).
:::

## 실습 (시뮬레이션)

아래 `▶ 실행` 블록은 강의 페이지 안에서 바로 돌아가며, 결과 패널에 로그와 함께 **2D·3D 리플레이**가 자동으로 표시됩니다(실습 3). 브라우저 Playground에는 카메라·YOLO가 없으므로, 인식 결과를 **사건 타임라인**(실습 1), **샘플 영상**(실습 2), **실제 자세로 흉내 낸 차선 인식**(실습 3)으로 대신합니다.

@btn[~/tools/playground.html] Playground 열기

### 실습 1: 전이표 FSM과 사건 타임라인

전이표·안전 전이·복귀·시간 제한·표지 기억 소비를 모두 담은 순수 Python FSM입니다. 사건은 인식 계층이 디바운싱을 마친 뒤라고 가정합니다.

```python run
# 미니 시티 FSM: 상태 × 사건 → 다음 상태 (+ 공통 안전 전이, 시간 제한, 이벤트 소비)
SPEED = {"LANE": 0.12, "SLOW": 0.06, "STOP": 0.0, "TURN_R": 0.08, "TURN_S": 0.10,
         "APPROACH": 0.06, "PARK": 0.0, "LOST": 0.0, "HOLD": 0.0, "DONE": 0.0}
TABLE = {("LANE", "crosswalk"): "SLOW",    ("SLOW", "red"): "STOP",
         ("STOP", "green"): "SLOW",        ("SLOW", "passed"): "LANE",
         ("LANE", "junction"): "TURN?",    # 표지 기억에 따라 TURN_R / TURN_S
         ("TURN_R", "straight"): "LANE",   ("TURN_S", "straight"): "LANE",
         ("LANE", "park_sign"): "APPROACH", ("APPROACH", "at_spot"): "PARK",
         ("PARK", "done"): "DONE"}
SAFETY = {"obstacle": "HOLD", "lane_lost": "LOST"}         # 어느 상태에서든 우선
RESUME = {"clear": "HOLD", "lane_found": "LOST"}           # 해제 사건 → 이전 상태로 복귀
TIMEOUT = {"STOP": 20.0, "LOST": 3.0, "HOLD": 10.0}        # 초과하면 ABORT

timeline = [(1.0, "sign_right"), (3.0, "crosswalk"), (3.5, "red"), (9.0, "green"),
            (11.0, "passed"), (12.0, "lane_lost"), (12.8, "lane_found"),
            (15.0, "junction"), (18.0, "straight"), (19.0, "obstacle"), (22.0, "clear"),
            (24.0, "junction"), (27.0, "straight"), (30.0, "park_sign"),
            (34.0, "at_spot"), (37.0, "done")]

state, prev, memo, t_in, dist, t_prev = "LANE", None, None, 0.0, 0.0, 0.0
for t, ev in timeline:
    if state in TIMEOUT and t - t_in > TIMEOUT[state]:
        print("%5.1fs 시간 초과 → ABORT" % t)
        break
    dist += SPEED[state] * (t - t_prev)
    t_prev = t
    if ev.startswith("sign_"):                             # 표지는 상태를 바꾸지 않고 기억만
        memo = ev[5:]
        print("%5.1fs %-10s 표지 기억: %s" % (t, ev, memo))
        continue
    if ev in SAFETY and state not in SAFETY.values():
        nxt, prev = SAFETY[ev], state
    elif ev in RESUME and state == RESUME[ev]:
        nxt = prev
    else:
        nxt = TABLE.get((state, ev))
    if nxt == "TURN?":
        nxt, memo = ("TURN_R" if memo == "right" else "TURN_S"), None   # 기억을 소비
    if nxt is None:
        print("%5.1fs %-10s 무시 (상태 %s)" % (t, ev, state))
        continue
    print("%5.1fs %-10s %-8s → %-8s v=%.2f  누적 %.2f m" % (t, ev, state, nxt, SPEED[nxt], dist))
    state, t_in = nxt, t
print("최종 상태:", state)
```

```text
  1.0s sign_right 표지 기억: right
  3.0s crosswalk  LANE     → SLOW     v=0.06  누적 0.36 m
  3.5s red        SLOW     → STOP     v=0.00  누적 0.39 m
  9.0s green      STOP     → SLOW     v=0.06  누적 0.39 m
 11.0s passed     SLOW     → LANE     v=0.12  누적 0.51 m
 12.0s lane_lost  LANE     → LOST     v=0.00  누적 0.63 m
 12.8s lane_found LOST     → LANE     v=0.12  누적 0.63 m
 15.0s junction   LANE     → TURN_R   v=0.08  누적 0.89 m
 18.0s straight   TURN_R   → LANE     v=0.12  누적 1.13 m
 19.0s obstacle   LANE     → HOLD     v=0.00  누적 1.25 m
 22.0s clear      HOLD     → LANE     v=0.12  누적 1.25 m
 24.0s junction   LANE     → TURN_S   v=0.10  누적 1.49 m
 27.0s straight   TURN_S   → LANE     v=0.12  누적 1.79 m
 30.0s park_sign  LANE     → APPROACH v=0.06  누적 2.15 m
 34.0s at_spot    APPROACH → PARK     v=0.00  누적 2.39 m
 37.0s done       PARK     → DONE     v=0.00  누적 2.39 m
최종 상태: DONE
```

- 1.0 s의 `sign_right`는 기억만 되고, 15.0 s 첫 교차로에서 **소비**되어 TURN_R, 24.0 s 두 번째 교차로에서는 기억이 비어 TURN_S가 됩니다. 원본의 `turn_right` 플래그는 한 번 서면 계속 남습니다.
- `lane_lost`(12.0 s)와 `obstacle`(19.0 s)은 속도를 0으로 만들고, 해제되면 **들어오기 전 상태**로 돌아갑니다.
- 서행 0.06 m/s가 차선 유지 0.12 m/s와 다릅니다(원본은 둘 다 0.1). 9.0 s의 `green`을 30.0 s로 옮기면 STOP 제한 20 s를 넘겨 ABORT가 나와야 합니다.

### 실습 2: 차선 중심 오차와 차선 상실 처리

샘플 `lane.png`(왼쪽 흰 선, 오른쪽 노란 선)로 차로 중심 오차를 구하고, 가림·반사광·차선 없음 프레임을 차례로 넣어 **3프레임 규칙**이 어떻게 동작하는지 봅니다.

```python run
import numpy as np
import cv2

img = cv2.imread("lane.png")                           # BGR 640x480, 왼쪽 흰 선·오른쪽 노란 선
BANDS = ((400, 420, 0.7), (330, 350, 0.2), (270, 290, 0.1))
V_LANE, V_LOST, MISS_MAX = 0.12, 0.0, 3                # 차선 3프레임 연속 미검출 → 정지

HALF = {400: 139, 330: 93, 270: 54}                    # 띠별 차로 반폭 [px] (정상 영상에서 잰 값)

def line_x(mask):                                      # 선 하나의 x. 없거나 너무 넓으면(반사광) None
    xs = np.nonzero(mask)[1]
    return None if len(xs) < 20 or np.ptp(xs) > 60 else xs.mean()

def lane_error(bgr):
    lab = cv2.cvtColor(bgr, cv2.COLOR_BGR2LAB)
    yel = cv2.inRange(lab, (135, 100, 160), (255, 150, 255))   # A09에서 이 영상에 맞춘 값
    wht = cv2.inRange(lab, (200, 118, 118), (255, 138, 138))
    err = 0.0
    for y0, y1, w in BANDS:
        xy, xw = line_x(yel[y0:y1]), line_x(wht[y0:y1])
        if xy is None and xw is None:
            return None                                # 두 선 모두 없음 → 미검출
        if xy is None:
            cx = xw + HALF[y0]                         # 한 선만 보이면 반폭으로 중심 추정
        elif xw is None:
            cx = xy - HALF[y0]
        else:
            cx = (xy + xw) / 2
        err += w * (cx - bgr.shape[1] / 2)
    return err

def frames():
    yield "정상", img
    yield "오른쪽 30px 이동", np.roll(img, 30, axis=1)
    g = img.copy()
    g[260:430, 400:640] = 0                            # 노란 선 쪽이 가려짐(그림자·소품)
    yield "노란선 가림", g
    g = img.copy()
    g[260:430, 380:640] = 235                          # 강한 반사광: 흰 덩어리
    yield "반사광", g
    for k in range(3):
        yield "차선 없음 %d" % (k + 1), np.full_like(img, 90)   # 맵 밖/그림자
    yield "복귀", img

miss = 0
for name, f in frames():
    e = lane_error(f)
    miss = 0 if e is not None else miss + 1
    if e is None:
        v, w, st = (V_LANE if miss < MISS_MAX else V_LOST), 0.0, "LOST" if miss >= MISS_MAX else "COAST"
    else:
        v, w, st = V_LANE, float(np.clip(-0.004 * e, -0.5, 0.5)), "LANE"
    print("%-14s 오차 %7s px  miss=%d  %-5s v=%.2f ω=%+.3f"
          % (name, "없음" if e is None else "%+.1f" % e, miss, st, v, w))
```

```text
정상             오차   +10.8 px  miss=0  LANE  v=0.12 ω=-0.043
오른쪽 30px 이동    오차   +40.8 px  miss=0  LANE  v=0.12 ω=-0.163
노란선 가림         오차   +10.7 px  miss=0  LANE  v=0.12 ω=-0.043
반사광            오차      없음 px  miss=1  COAST v=0.12 ω=+0.000
차선 없음 1        오차      없음 px  miss=2  COAST v=0.12 ω=+0.000
차선 없음 2        오차      없음 px  miss=3  LOST  v=0.00 ω=+0.000
차선 없음 3        오차      없음 px  miss=4  LOST  v=0.00 ω=+0.000
복귀             오차   +10.8 px  miss=0  LANE  v=0.12 ω=-0.043
```

- **노란 선이 가려져도** 흰 선 + 차로 반폭으로 중심을 추정해 정상과 거의 같은 오차(+10.7 px)를 냅니다. 반폭은 원근 때문에 띠마다 다릅니다(139/93/54 px).
- **반사광**은 흰색 임계값을 통과하는 넓은 덩어리를 만듭니다. 픽셀 분포 폭(`np.ptp`)이 60 px를 넘으면 선이 아니라고 보고 버려서, 엉뚱한 +280 px 오차로 급조향하는 일을 막습니다(검사를 지우고 실행해 비교해 보세요).
- 미검출 1~2프레임은 조향 0으로 **관성 주행(COAST)**하고, 3프레임째부터 정지합니다. 30 fps에서 0.1 s, 0.12 m/s로 1.2 cm만 더 가고 멈춥니다. 원본은 이 상황에서 마지막 명령을 무한히 유지합니다.

### 실습 3: 도시 순환 주행 — 신호등·횡단보도·교차로·주차

`empty` 월드에 차로 중심선 네 점으로 된 순환 도로를 두고, **시간으로 바뀌는 가상 신호등**(0~5 s 빨강)과 정지선(x = −0.9 m), 횡단보도(x −0.85 ~ −0.65 m), 상자 세 개로 둘러싼 주차 칸을 만듭니다. 차선 인식은 시뮬 전용 `sim.pose()`로 차로 중심선에 대한 횡오차·방향 오차를 계산해 대신하고(실물에서는 `LaneDetector`), 라이다 `/scan`으로 전방 장애물을 감시합니다. 교차로는 반경 0.3 m 호(0.15 m/s, 0.5 rad/s)로 돌고, 주차는 메카넘 옆걸음(원본 `park_action()`의 `linear.y = −0.2`)입니다.

```python run
import math
import time
import rclpy
from rclpy.node import Node
from geometry_msgs.msg import Twist
from sensor_msgs.msg import LaserScan
import mentorpi_sim as sim                                    # 시뮬 전용
sim.setup(chassis="mecanum", world="empty", duration=30.0,
          start=(-1.3, -0.9, 0.0), noise=True)                # 시뮬 전용
for bx in (-0.12, -0.68):
    sim.add_box(bx, 0.85, 0.06, 0.4)                           # 시뮬 전용: 주차 칸 옆벽
sim.add_box(-0.4, 1.1, 0.62, 0.06)                             # 시뮬 전용: 주차 칸 뒷벽

ROUTE = [(-1.3, -0.9), (0.5, -0.9), (0.5, 0.4), (-0.4, 0.4)]  # 순환 도로 차로 중심선
STOP_X, CROSS, SLOT = -0.9, (-0.85, -0.65), (-0.4, 0.85)       # 정지선, 횡단보도, 주차 칸 중심
V = {"LANE": 0.2, "SLOW": 0.1, "STOP": 0.0, "TURN": 0.15, "PARK": 0.0}
light = lambda t: "red" if t < 5.0 else "green"                # 가상 신호등 (시간으로 바뀜)

class CityDriver(Node):
    def __init__(self):
        super().__init__("city_driver")
        self.pub = self.create_publisher(Twist, "/cmd_vel", 1)
        self.create_subscription(LaserScan, "/scan", self.on_scan, 1)
        self.state, self.seg, self.front, self.violation, self.slow_v = "LANE", 0, 9.9, False, 0.0
        self.t0 = self.t_enter = time.time()
        self.create_timer(0.05, self.step)

    def on_scan(self, msg):
        n = len(msg.ranges)
        self.front = min(msg.ranges[n // 2 - 15: n // 2 + 16])   # 전방 ±15° (인덱스 n/2 = 0°)

    def go(self, s):
        print("%5.1fs  %-5s → %s" % (time.time() - self.t0, self.state, s))
        self.state, self.t_enter = s, time.time()

    def step(self):
        t, st = time.time() - self.t0, self.state
        x, y, th = sim.pose()                                  # 시뮬 전용: 차선 인식 대용
        (ax, ay), (bx, by) = ROUTE[self.seg], ROUTE[self.seg + 1]
        hd = math.atan2(by - ay, bx - ax)
        e = -(x - ax) * math.sin(hd) + (y - ay) * math.cos(hd)   # 횡오차(왼쪽 +)
        psi = math.atan2(math.sin(th - hd), math.cos(th - hd))    # 방향 오차
        left = (bx - x) * math.cos(hd) + (by - y) * math.sin(hd)  # 구간 끝까지 거리
        if self.seg == 0 and x > STOP_X and light(t) == "red":
            self.violation = True                               # 빨간불에 정지선 통과
        if st == "LANE" and self.seg == 0 and light(t) == "red" and 0 < STOP_X - x < 0.06:
            self.go("STOP")
        elif st == "STOP" and light(t) == "green" or st == "LANE" and CROSS[0] < x < CROSS[1] and self.seg == 0:
            self.go("SLOW")
        elif st == "SLOW" and x >= CROSS[1]:
            self.go("LANE")
        elif st == "LANE" and self.seg < len(ROUTE) - 2 and left < 0.3:
            self.go("TURN")                                     # 반경 0.3 m 호로 회전
        elif st == "LANE" and left < 0.02:
            self.go("PARK")
        cmd, st = Twist(), self.state
        if st == "TURN":
            nx, ny = ROUTE[self.seg + 2]
            err = math.atan2(ny - by, nx - bx) - th
            err = math.atan2(math.sin(err), math.cos(err))
            cmd.linear.x, cmd.angular.z = V["TURN"], math.copysign(0.5, err)
            if abs(err) < 0.05:
                self.seg += 1
                self.go("LANE")
        elif st == "PARK":                                     # 메카넘: 오른쪽으로 평행 이동
            cmd.linear.y = -0.2
            if time.time() - self.t_enter > 0.45 / 0.2:
                self.finish(t, x, y, psi)
        elif st in ("LANE", "SLOW"):
            cmd.linear.x = V[st] if self.front > 0.25 else 0.0     # 전방 장애물이면 정지
            cmd.angular.z = max(-0.5, min(0.5, -3.0 * e - 1.5 * psi))
            if st == "SLOW":
                self.slow_v = max(self.slow_v, cmd.linear.x)
        self.pub.publish(cmd)

    def finish(self, t, x, y, psi):
        ex, ey = x - SLOT[0], y - SLOT[1]
        park = 20 if max(abs(ex), abs(ey)) <= 0.03 and abs(math.degrees(psi)) <= 10 else 15
        score = 20 + (0 if self.violation else 15) + (10 if self.slow_v <= 0.1 else 0) + 10 + park
        print("완주 %.1f s | 주차 오차 x %+.2f y %+.2f m, 자세 %+.1f°" % (t, ex, ey, math.degrees(psi)))
        print("신호 위반 %s, 서행 최고 %.2f m/s → 시뮬 채점 %d / 75" % (self.violation, self.slow_v, score))
        raise SystemExit

rclpy.init()
node = CityDriver()
try:
    rclpy.spin(node)
except (KeyboardInterrupt, SystemExit):
    pass
finally:
    node.pub.publish(Twist())
    node.destroy_node()
    rclpy.shutdown()
```

(대략)

```text
  2.0s  LANE  → STOP
  5.0s  STOP  → SLOW
  8.1s  SLOW  → LANE
 12.6s  LANE  → TURN
 15.8s  TURN  → LANE
 19.7s  LANE  → TURN
 22.9s  TURN  → LANE
 26.1s  LANE  → PARK
완주 28.4 s | 주차 오차 x -0.00 y -0.02 m, 자세 +0.5°
신호 위반 False, 서행 최고 0.10 m/s → 시뮬 채점 75 / 75
```

결과 패널의 2D·3D 리플레이에서 확인할 점:

- 2.0 s에 정지선 6 cm 앞에서 STOP, 5.0 s 초록불에 SLOW로 출발합니다. 빨간불 동안 정지선을 넘지 않았으므로 신호 위반이 `False`입니다.
- 서행 구간의 최고 속도가 0.10 m/s로 차선 유지(0.2 m/s, 시뮬 `/cmd_vel` 상한)와 구분됩니다. 30 s 안에 끝내려고 시뮬에서는 실물보다 빠르게 달립니다.
- 두 번의 TURN이 각각 약 3.2 s입니다. 90°를 0.5 rad/s로 돌면 $π/2 ÷ 0.5 ≈ 3.1$ s이므로, 각속도 한계가 곧 코너 시간의 하한입니다.
- 주차 오차가 수 cm 이내입니다. 시뮬에서도 시간(0.45/0.2 s) 기반이지만, 실물에서는 바퀴 미끄러짐 때문에 같은 시간이 다른 거리가 되므로 `/odom` 거리로 바꿉니다(실물 코드 참고).
- `light()`의 5.0을 8.0으로 바꾸면 완주가 30 s를 넘겨 DONE 줄이 안 나옵니다. 시간 점수와 신호 대기의 관계를 체감해 보세요. `sim.add_box(0.0, -0.9, 0.1, 0.1)`로 차로에 상자를 두면 전방 감시로 멈춥니다(F7).

2D 시뮬레이터의 **라인 트레이싱** 모드에서는 트랙 월드의 커브마다 "LANE → TURN → LANE" 사건이 일어나는 위치를 관찰하고, 실습 1의 타임라인을 실제 코스 길이에 맞게 고쳐 보세요.

@btn[~/sim/index.html] 2D 시뮬레이터 열기

## 실습 (실물)

:::safety 실물 챌린지 안전 수칙
- 코스 둘레 1 m를 비우고, **심판 1명은 항상 로봇 옆에서 따라가며** 들어 올릴 준비를 합니다. 들어 올리면 개입 감점이지만 안전이 먼저입니다.
- 팀 노드의 속도표는 LANE 0.12 / SLOW 0.06 / 회전 0.45 rad/s를 넘기지 않습니다. 시간 점수를 위해 한계를 올리지 않습니다.
- 처음 실행은 반드시 **바퀴를 띄운 상태**(받침대)에서 `~/state` 전이와 명령 부호를 확인한 뒤 바닥에 내립니다.
- 비상 정지 순서: `ros2 service call /city_fsm/set_running std_srvs/srv/SetBool "{data: false}"` → 노드 터미널 `Ctrl+C` → 전원 스위치.
- 주차 칸 벽·소품은 가벼운 종이 상자로 만듭니다. 원본 `park_action()`의 애커만 각속도(0.573 rad/s)는 권장 한계를 넘으니 애커만 팀은 속도를 낮춰 씁니다.
:::

### 1. 기준선: 원본 통합 주행

```bash
~/.stop_ros.sh
ros2 launch example self_driving.launch.py
# 다른 터미널: 결과 영상과 기록
rqt
ros2 bag record -o baseline /controller/cmd_vel /yolov5_ros2/object_detect
```

### 2. 팀 노드 실행

원본 launch 대신 구성 요소를 따로 띄우고 팀 노드를 실행합니다. `self_driving.launch.py`가 켜 두는 카메라·컨트롤러·YOLOv5와 같은 구성에, 안전 감시용 라이다를 더한 것입니다.

```bash
~/.stop_ros.sh
ros2 launch peripherals depth_camera.launch.py                                  # 터미널 1
ros2 launch controller controller.launch.py                                     # 터미널 2
ros2 launch peripherals lidar.launch.py                                         # 터미널 3
ros2 launch yolov5_ros2 yolov5_ros2.launch.py model:="traffic_signs_640s_7_0"   # 터미널 4
ros2 service call /yolov5/start std_srvs/srv/Trigger {}                         # 원본과 같은 시작 요청
python3 ~/city_fsm.py                                                           # 터미널 5
```

```bash
ros2 topic echo /city_fsm/state                       # 전이 로그
ros2 bag record -o run1 /city_fsm/state /controller/cmd_vel /yolov5_ros2/object_detect
```

### 3. 팀 노드 코드 (`city_fsm.py`)

원본 `LaneDetector`와 YOLOv5 메시지를 그대로 쓰고, 위의 전이표·사건 정의·안전 한계를 구현한 골격입니다. 메카넘 섀시 기준이며, 애커만은 `drive()`의 각속도를 $v·tan δ / 0.145$로 바꾸고 주차를 A11의 3단계 동작으로 바꿔야 합니다.

```python robot
#!/usr/bin/env python3
import math
import numpy as np
import rclpy
from rclpy.node import Node
from rclpy.qos import qos_profile_sensor_data
from cv_bridge import CvBridge
from sensor_msgs.msg import Image, LaserScan
from geometry_msgs.msg import Twist
from nav_msgs.msg import Odometry
from std_msgs.msg import String
from std_srvs.srv import SetBool
from interfaces.msg import ObjectsInfo
from example.self_driving.lane_detect import LaneDetector

SPEED = {"LANE": 0.12, "SLOW": 0.06, "STOP": 0.0, "TURN_R": 0.08, "TURN_S": 0.10,
         "APPROACH": 0.06, "PARK": 0.0, "LOST": 0.0, "HOLD": 0.0, "DONE": 0.0, "ABORT": 0.0}
TABLE = {("LANE", "crosswalk"): "SLOW", ("SLOW", "red"): "STOP", ("STOP", "green"): "SLOW",
         ("SLOW", "passed"): "LANE", ("LANE", "junction"): "TURN?",
         ("TURN_R", "straight"): "LANE", ("TURN_S", "straight"): "LANE",
         ("LANE", "park_sign"): "APPROACH", ("APPROACH", "at_spot"): "PARK", ("PARK", "done"): "DONE"}
NEED = {"crosswalk": 3, "red": 1, "green": 5, "sign_right": 5, "junction": 6,
        "park_sign": 5, "at_spot": 5, "lane_lost": 3, "obstacle": 2}
TIMEOUT = {"STOP": 20.0, "LOST": 3.0, "HOLD": 10.0}
SETPOINT, KP, KD, W_MAX, PARK_DIST = 130, 0.004, 0.002, 0.3, 0.38

class CityFsm(Node):
    def __init__(self):
        super().__init__('city_fsm')
        self.state, self.prev, self.memo, self.running = "LANE", "LANE", None, True
        self.hits, self.last_err, self.odom, self.park0 = {}, 0.0, (0.0, 0.0), None
        self.t_in = self.t_img = self.now()
        self.lane, self.bridge = LaneDetector("yellow"), CvBridge()
        self.pub = self.create_publisher(Twist, '/controller/cmd_vel', 1)
        self.state_pub = self.create_publisher(String, '~/state', 10)
        self.create_subscription(Image, '/ascamera/camera_publisher/rgb0/image', self.on_image, 1)
        self.create_subscription(ObjectsInfo, '/yolov5_ros2/object_detect', self.on_objects, 1)
        self.create_subscription(LaserScan, '/scan_raw', self.on_scan, qos_profile_sensor_data)
        self.create_subscription(Odometry, '/odom', self.on_odom, 10)
        self.create_service(SetBool, '~/set_running', self.on_set_running)
        self.create_timer(0.1, self.watchdog)

    def now(self):
        return self.get_clock().now().nanoseconds * 1e-9
    def seen(self, name, ok):                        # 디바운싱: NEED번 연속이면 사건 발생
        self.hits[name] = self.hits.get(name, 0) + 1 if ok else 0
        if self.hits[name] == NEED.get(name, 1):
            self.event(name)
    def event(self, ev):
        if ev == "sign_right":
            self.memo = "right"
            return
        if ev in ("lane_lost", "obstacle") and self.state not in ("LOST", "HOLD", "DONE", "ABORT"):
            nxt, self.prev = ("LOST" if ev == "lane_lost" else "HOLD"), self.state
        elif (ev, self.state) in (("lane_found", "LOST"), ("clear", "HOLD")):
            nxt = self.prev
        else:
            nxt = TABLE.get((self.state, ev))
        if nxt == "TURN?":
            nxt, self.memo = ("TURN_R" if self.memo == "right" else "TURN_S"), None
        if nxt:
            self.go(nxt, ev)
    def go(self, nxt, why):
        self.get_logger().info('%s --%s--> %s' % (self.state, why, nxt))
        self.state, self.t_in = nxt, self.now()
        self.state_pub.publish(String(data=nxt))
        if nxt == "PARK":
            self.park0 = self.odom
    def on_objects(self, msg):
        cw = max([(o.box[1] + o.box[3]) / 2 for o in msg.objects if o.class_name == 'crosswalk'] + [0])
        names = {o.class_name for o in msg.objects}
        self.seen("crosswalk", cw > 70)
        self.seen("red", "red" in names)
        self.seen("green", "green" in names)
        self.seen("sign_right", "right" in names)            # go 표지는 기억할 필요 없음
        self.seen("park_sign", "park" in names and cw > 135)
        self.seen("at_spot", self.state == "APPROACH" and cw > 180)

    def on_scan(self, msg):
        near = [r for i, r in enumerate(msg.ranges)
                if abs(msg.angle_min + i * msg.angle_increment) < math.radians(20) and 0.05 < r < 12.0]
        d = min(near + [9.9])
        self.seen("obstacle", d < 0.25)
        if self.state == "HOLD" and d > 0.35:
            self.event("clear")
    def on_odom(self, msg):
        self.odom = (msg.pose.pose.position.x, msg.pose.pose.position.y)
    def on_set_running(self, req, res):
        self.running = req.data
        self.pub.publish(Twist())
        res.success = True
        return res
    def on_image(self, msg):
        self.t_img = self.now()
        img = np.array(self.bridge.imgmsg_to_cv2(msg, "rgb8"), dtype=np.uint8)
        _, _, lane_x = self.lane(self.lane.get_binary(img), img.copy())
        self.seen("lane_lost", lane_x < 0)
        if lane_x >= 0 and self.state == "LOST":
            self.event("lane_found")
        self.seen("junction", self.state == "LANE" and lane_x > 150)
        if self.state in ("TURN_R", "TURN_S") and 0 <= lane_x <= 150 and self.now() - self.t_in > 2.0:
            self.event("straight")
        if self.state == "SLOW" and self.now() - self.t_in > 0.4 / SPEED["SLOW"]:
            self.event("passed")
        self.drive(lane_x)
    def drive(self, lane_x):
        t = Twist()
        if self.running and self.state == "PARK":            # 폐루프 주차: 옆으로 0.38 m
            moved = math.dist(self.odom, self.park0)
            t.linear.y = -0.15 if moved < PARK_DIST else 0.0
            if moved >= PARK_DIST:
                self.event("done")
        elif self.running and SPEED[self.state] > 0 and lane_x >= 0:
            t.linear.x = SPEED[self.state]
            if self.state == "TURN_R":
                t.angular.z = -0.45                          # 원본 회전 각속도
            else:
                err = SETPOINT - lane_x
                t.angular.z = float(np.clip(KP * err + KD * (err - self.last_err), -W_MAX, W_MAX))
                self.last_err = err
        self.pub.publish(t)                                  # 정지도 명시적으로 발행
    def watchdog(self):
        if self.now() - self.t_img > 0.5:
            self.pub.publish(Twist())                        # 영상이 끊기면 정지
        if self.state in TIMEOUT and self.now() - self.t_in > TIMEOUT[self.state]:
            self.go("ABORT", "timeout")

def main():
    rclpy.init()
    node = CityFsm()
    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    finally:
        node.pub.publish(Twist())
        node.destroy_node()
        rclpy.shutdown()

if __name__ == '__main__':
    main()
```

코드에서 짚을 점:

- `drive()`는 **모든 프레임에서** Twist를 발행합니다. 속도가 0이어야 하는 상태(STOP·LOST·HOLD·ABORT)에서도 0을 명시적으로 보내므로, 원본처럼 마지막 명령이 남지 않습니다.
- `seen()`은 연속 횟수가 `NEED`와 **같아지는 순간 한 번만** 사건을 냅니다. 빨간불이 계속 보여도 `red` 사건은 한 번이고, 사라졌다가 다시 보여야 다음 사건이 납니다.
- 표지 기억 `memo`는 `junction`에서 읽힌 뒤 `None`이 됩니다. `go` 표지는 기억할 필요가 없어서 무시합니다(기억이 없으면 직진).
- 주차 거리는 `/odom`(EKF 출력, 옆걸음 포함 — M03)으로 잽니다. 속도를 원본 0.2에서 0.15로 낮췄지만 거리 기반이라 위치는 같습니다.

### 4. 튜닝 순서

한 번에 하나만 바꾸고 표로 기록합니다. 권장 순서는 ① `SETPOINT`(직선에서 차로 가운데) → ② `KP`/`KD`(떨림 없이 커브 추종) → ③ `NEED["junction"]`(커브와 교차로 구분) → ④ `NEED["green"]`(3 s 출발 제한 안에서 최대) → ⑤ `PARK_DIST` → 마지막에 ⑥ `SPEED["LANE"]`입니다. 속도는 다른 파라미터를 모두 흔들므로 맨 나중에 올립니다.

:::check 체크포인트 (실물)
- [ ] 바퀴를 띄운 상태에서 모든 전이를 한 번씩 일으켜 `~/state` 로그로 확인했다.
- [ ] 차선 가림 시험(F6)과 상자 시험(F7)을 각 3회 통과했다.
- [ ] 본선 2회 채점표와 rosbag이 있다.
:::

## 평가 기준

| 영역 | 기준 | 배점 |
|---|---|---|
| 주행 성능 | 본선 최고 점수 × 0.4 (챌린지 100점 → 40점 환산) | 40 |
| 설계 | 노드·토픽 그래프, 전이표, 사건 정의표가 코드와 일치 | 15 |
| 개선 검증 | 원본 약점 3개 이상에 대해 개선 전후 수치(rosbag·측정) 제시 | 15 |
| 안전 | 비상 정지·차선 상실 정지·장애물 정지 시연, 속도 한계 준수 | 15 |
| 코드 품질 | 파라미터를 상단 상수/ROS 파라미터로 분리, 단일 출력, 로그 | 10 |
| 발표·회고 | 실패 사례와 원인 분석, 역할별 기여 | 5 |
| **합계** | | **100** |

## 확장 과제

- **애커만 섀시 대응**: `drive()`의 각속도를 조향각으로 바꾸고, A11의 3단계 주차를 `/odom` 자세로 폐루프화하세요. 최소 회전 반경 0.262 m가 코스 코너보다 큰지 먼저 확인합니다.
- **원근 변환 오차**: A09의 호모그래피로 lane_x 대신 미터 단위 횡오차를 쓰면, 카메라 각도가 바뀌어도 `SETPOINT`·이득을 다시 맞출 필요가 줄어듭니다.
- **신호등 색 교차 검증**: YOLO `red`/`green` 박스 안의 HSV 픽셀 수로 한 번 더 확인하는 함수(A10)를 사건 생성에 넣으세요.
- **ROS 2 파라미터화**: `SPEED`, `NEED`, `TIMEOUT`을 `declare_parameter`로 바꾸고, 시행 사이에 `ros2 param set`으로 조정하세요(재시작 없이 5분 안에 튜닝).
- **행동 트리로 재설계**: 같은 기능을 BehaviorTree(예: `py_trees`)로 짜 보고, 전이표 FSM과 가독성·확장성을 비교하세요.

## 자주 나는 오류와 해결

| 증상 | 원인 | 해결 |
|---|---|---|
| 노드 시작 직후 로봇이 멈춰 있고 `~/state`는 LANE | 영상이 안 와서 워치독이 정지 명령 | `ros2 topic hz /ascamera/camera_publisher/rgb0/image`, 카메라 launch 확인 |
| `ModuleNotFoundError: example.self_driving` | 작업공간 환경이 적용되지 않은 셸 | 컨테이너 셸에서 `source ~/ros2_ws/install/setup.bash` 후 실행 |
| `/yolov5_ros2/object_detect`가 비어 있음 | 모델 로딩 중이거나 조명·거리 문제 | YOLO 터미널 로그 확인, `/result_img`로 검출 확인 |
| 빨간불 앞에서 서지 않음 | `crosswalk` 사건이 늦어 SLOW에 들어가기 전에 신호를 봄 | 횡단보도 y 문턱(70)을 낮추거나, (LANE, red) → STOP 칸 추가 검토 |
| 초록불인데 3 s 넘게 출발 안 함 | `NEED["green"]`이 크거나 초록 LED 과노출 | 5 → 3, 카메라 노출·신호등 각도 조정 |
| 커브마다 TURN_S/TURN_R로 들어감 | 일반 커브도 lane_x > 150이 6프레임 지속 | 교차로 구간에서만 `junction`을 허용(표지 본 뒤 N초), 문턱 조정 |
| 회전이 끝나지 않고 계속 돎 | `straight` 조건(2 s 경과 + lane_x ≤ 150)이 안 맞음 | 회전 후 선이 보이는 x 범위를 로그로 확인해 조건 수정 |
| 주차 후 칸을 지나침 | `/odom` 대신 `/odom_raw`를 구독(옆걸음 미반영) | `/odom`(EKF 출력) 사용, `controller.launch.py` 실행 확인 |
| 코스 벽을 장애물로 보고 HOLD 반복 | ±20° 섹터가 코너에서 벽을 봄 | 섹터 각도·거리 축소, 코스 둘레 벽을 낮게 |
| ABORT로 끝남 | STOP 20 s 초과(심판이 신호를 늦게 바꿈) 등 | 로그의 `timeout` 원인 확인, 규칙상 3~8 s 안에 바꿈 |

## 과제

:::task 과제
1. **(기본)** 실습 1의 타임라인에 "STOP 중 `lane_lost`"를 넣고, 초록불이 LOST 중에 오면 어떻게 되는지 확인하세요. 그 동작이 안전한지, 전이표를 어떻게 고쳐야 하는지 제안하세요.
2. **(기본)** 실습 2에서 `MISS_MAX`를 1, 3, 10으로 바꿔 보고, 30 fps·0.12 m/s에서 각각 정지 전까지 몇 cm를 가는지 계산해 표로 정리하세요.
3. **(심화)** 실습 3에 `sim.add_box(0.5, -0.3, 0.12, 0.12)`로 두 번째 구간에 장애물을 놓고, 장애물이 사라지는 시점을 흉내 내기 위해 HOLD 중 일정 시간이 지나면 우회(메카넘 옆걸음)하는 상태를 추가하세요.
4. **(심화)** 본선 rosbag에서 `/controller/cmd_vel`을 읽어 서행 구간 최고 속도, 조향 부호 반전 횟수, 완주 시간을 자동으로 계산하는 **채점 보조 스크립트**를 만드세요.
:::

## 참고자료

- 원본 교재: *17. Autonomous Driving Lesson* — Map Laying and Prop Installation, Lesson 1 Lane Keeping, Lesson 2 Road Sign Detection, Lesson 3 Traffic Light Recognition, Lesson 4 Turning Decision Making, Lesson 5 Autonomous Parking, Lesson 6 Integrated Application
- 원본 소스: `src/example/example/self_driving/self_driving.py`, `lane_detect.py`, `self_driving.launch.py`, `src/yolov5_ros2/yolov5_ros2/yolo_detect.py`, `src/yolov5_ros2/launch/yolov5_ros2.launch.py`
- 이 강의: A03(라인 추종 PID), A09(차선 유지), A10(표지·신호등), A11(회전·주차·FSM), M06(라이다 반응형 행동)
- ROS 2 Humble — rosbag2 기록·재생: https://docs.ros.org/en/humble/Tutorials/Beginner-CLI-Tools/Recording-And-Playing-Back-Data/Recording-And-Playing-Back-Data.html
- ROS 2 Humble — 파라미터: https://docs.ros.org/en/humble/Tutorials/Beginner-CLI-Tools/Understanding-ROS2-Parameters/Understanding-ROS2-Parameters.html
- Duckietown — 교육용 소형 자율주행 도시 플랫폼: https://www.duckietown.org
- py_trees — Python 행동 트리 라이브러리: https://py-trees.readthedocs.io
