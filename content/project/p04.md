---
id: p04
track: project
title: 사람 추종 카트 (비전+라이다 융합)
duration: 240
level: 고급
requires: [a05, a06, m06, m12]
tools: [sim3d, sim, playground]
---

## 학습 목표

- 카메라(방위·신원은 좋고 거리는 약함)와 라이다(거리는 좋고 신원은 없음)의 **상보적 특성**을 설명하고, 카메라 방위로 라이다 점을 고르는 **게이팅(gating) 융합**을 구현합니다.
- α-β 필터(정상상태 칼만 필터의 단순형)로 사람의 위치·속도를 추정하고, 추정 속도를 **피드포워드**로 써서 추종 거리의 정상상태 오차를 없앱니다.
- 0.8 m 추종 거리 제어, 장애물 정지·회피, 목표 상실 시 탐색, 제스처 시작·정지를 하나의 상태 기계로 묶습니다.
- 원본 `lidar_controller.py` 추종 모드와 `body_control`·`body_track` 예제의 한계를 데이터로 보이고, 개선된 Follow-me 노드를 실물에서 시험·평가합니다.

## 프로젝트 개요와 요구사항

마트나 창고에서 짐을 싣고 주인을 따라오는 **Follow-me 카트**를 MentorPi로 만듭니다. M06의 라이다 추종은 "가장 가까운 물체"를 따라가므로 벽·의자·다른 사람에게 쉽게 넘어가고, A06의 MediaPipe 예제는 사람을 알아보지만 거리를 모릅니다. 두 센서를 합쳐 **"그 사람"을, 적당한 거리에서, 안전하게** 따라가는 것이 목표입니다.

| 차시 | 활동 | 산출물 |
|---|---|---|
| 1 | 원본 라이다 추종·자세 제어 실행, 실패 사례 수집, 요구사항 합의 | 실패 영상, 요구사항표 |
| 2 | 게이팅·α-β 필터 프로토타입(실습 1·2), 카메라 방위 교정 | 오차 비교표, fx·라이다 각도 오프셋 |
| 3 | 상태 기계·탐색·장애물(실습 3), 실물 통합 | 전이 로그 |
| 4 | 시험 코스 평가, 튜닝·발표 | 평가표, rosbag, 발표 자료 |

| 역할 | 책임 |
|---|---|
| 비전 | MediaPipe Pose → 방위 `b_cam`, 제스처, 신원(몸통 색) |
| 융합 | 라이다 게이팅, α-β 필터, 좌표 변환(odom) |
| 제어·안전 | 거리·방위 제어, 장애물 판단, 상태 기계, 속도 제한 |
| 시험·기록 | 시험 코스 준비, 측정(줄자·rosbag), 평가표 |

### 기능 요구사항

| ID | 요구사항 | 확인 방법 |
|---|---|---|
| F1 | 주인이 **오른팔을 1 s 들면** 등록·추종 시작, **양팔을 들면** 정지 | 전이 로그 |
| F2 | 직선 보행(0.3 m/s 이하)에서 표면 거리 0.8 ± 0.15 m 유지 | rosbag의 추정 거리, 줄자 |
| F3 | 코너(90°)를 돌아도 추종 유지 | 시험 코스 |
| F4 | 주인이 기둥 뒤로 1~3 s 가려져도 다시 찾아 추종 | 전이 로그(SEARCH → FOLLOW) |
| F5 | 다른 사람이 사이를 지나가도 주인을 유지 | 영상, 로그 |
| F6 | 사람이 아닌 장애물이 전방 0.35 m 안이면 전진 정지(메카넘은 옆으로 비킴) | 상자 시험 |
| F7 | 10 s 동안 못 찾으면 정지하고 부저로 알림 | 로그 |

### 안전 한계

| 항목 | 값 | 근거 |
|---|---|---|
| 최대 선속도 | 전진 0.2 m/s, 후진 0.1 m/s | 강의 권장 한계, 뒤는 라이다만 봄 |
| 최대 각속도 | 0.5 rad/s | 원본 `app_cmd_vel_callback` 기준 |
| 사람과 최소 거리 | 0.4 m 미만이면 무조건 정지 | 발·짐수레와 부딪힘 방지 |
| 장애물 정지 | 사람 게이트 밖, 전방 ±35°, 0.35 m | 0.2 m/s에서 반응 0.15 s + 제동 ≈ 0.05 m, 차체 반경 0.13 m + 여유 |
| 측정 유효 시간 | 카메라 결과가 0.2 s보다 오래되면 사용 안 함 | MediaPipe 처리 지연 |
| 탐색 | 제자리 회전 0.3 rad/s, 최대 10 s | 사람 쪽으로 돌진하지 않음 |

### 원본 예제의 한계

| 원본 | 하는 일 | 한계 | 이 프로젝트 |
|---|---|---|---|
| `app/lidar_controller.py` mode 2 | ±45° 창의 **최근접점**을 PD로 추종, 목표 thr/2(기본 0.3 m) | 가장 가까운 물체가 사람이라는 보장이 없음, 움직이는 목표에 $v_t/K_p$ 정상상태 오차(M06) | 카메라 게이트로 대상 선택, 피드포워드 |
| `body_control` | Pose로 팔·다리 들기 판정 → 1초 이동 | 추종 기능 없음, 0.3 m/s 명령 | 같은 Pose 결과를 제스처·방위로 재사용 |
| `body_track` | YOLOv5 `person` 박스 중심의 깊이로 거리 PID(목표 150 → 1.5 m) | TensorRT 엔진(`yolov5s.engine`) 전제, 소스상 깊이 콜백이 RGB 토픽을 구독 | 라이다 거리로 대체 |
| `hand_track` | 손 중심을 팬·틸트 서보로 추종 | 섀시는 움직이지 않음 | 방위 계산 방식만 참고 |

## 시스템 설계

### 센서의 성격과 융합 방식

| | 카메라 + MediaPipe | 2D 라이다 (LD19) |
|---|---|---|
| 방위 | 좋음 (픽셀 → 각도, σ ≈ 1~2°) | 좋음 (1° 간격) |
| 거리 | 나쁨 (몸 크기로 추정, 수십 % 오차) | 매우 좋음 (cm 단위) |
| 신원 | 사람인지, 누구인지(옷 색) 앎 | 모름 — 다리·의자·벽이 모두 점 |
| 시야 | 좁음 (가정 ±30°) | 넓음 (360°, 뒤쪽 일부 가림) |
| 주기·지연 | 수~십수 fps, 처리 지연 큼 | 10 Hz, 지연 작음 |

한 센서의 약점을 다른 센서가 정확히 메우는 조합입니다. 이 프로젝트는 **카메라가 "어느 방향의 무엇"을 정하고, 라이다가 "얼마나 멀리"를 잰다**는 분업을 씁니다. 이런 방식을 결정 수준(late) 융합 중에서도 **게이팅 융합**이라고 합니다.

@fig[p04-graph] Follow-me 노드의 데이터 흐름. 카메라 방위가 라이다 측정을 고르고, 필터 출력이 상태 기계와 제어기로 갑니다.

| 토픽 | 타입 | 방향 | 용도 |
|---|---|---|---|
| `/ascamera/camera_publisher/rgb0/image` | `sensor_msgs/Image` | 구독 | MediaPipe Pose |
| `/ascamera/camera_publisher/rgb0/camera_info` | `sensor_msgs/CameraInfo` | 1회 확인 | 초점거리 fx, 주점 cx |
| `/scan_raw` | `sensor_msgs/LaserScan` | 구독 (sensor QoS) | 게이트 측정, 전방 안전 섹터 |
| `/odom` | `nav_msgs/Odometry` | 구독 | 필터 좌표계(로봇이 움직여도 사람 위치가 연속) |
| `/controller/cmd_vel` | `geometry_msgs/Twist` | 발행 | 속도 명령(노드 안에서 제한) |
| `~/state` | `std_msgs/String` | 발행 | 전이 로그 |

### 1단계: 영상 x → 방위

핀홀 모델에서 영상 가로 좌표 $u$에 찍힌 점의 방위(로봇 기준, 왼쪽 +)는

```text
b_cam = −atan((u − cx) / fx)        u: 어깨 중점의 x [px], fx·cx: camera_info의 K[0], K[2]
```

영상 오른쪽(u > cx)은 로봇의 오른쪽(음의 방위)입니다. 원본 `body_control`은 결과 영상을 좌우 반전(`cv2.flip`)해서 보여 주므로, **반전하기 전 좌표**로 계산해야 부호가 맞습니다. 추종점으로 코·얼굴 대신 **어깨 중점**을 쓰는 이유는 고개를 돌려도 흔들리지 않기 때문입니다(A06의 손바닥 중심과 같은 발상).

### 2단계: 게이팅

@fig[p04-fusion] 방위 게이트(b_cam ± 8°) 안의 라이다 점만 후보가 되고, 예측 거리 r̂ ± 0.3 m를 벗어난 점(의자 다리)은 거부됩니다.

1. **방위 게이트**: $|a_i − b_{cam}| < 8°$인 빔만 후보로 둡니다. 8°는 카메라 방위 오차(σ 2°)의 약 4배, 그리고 0.8 m 거리에서 사람 몸통 반폭(≈ 0.2 m → 14°)의 절반쯤입니다.
2. **거리 게이트**: 후보 중 최근접 거리 $r$이 필터 예측 $\hat r$에서 0.3 m 넘게 벗어나면 측정을 버립니다. 사람과 같은 방향에 있는 **더 가까운 물체**(의자 다리, 기둥 모서리)를 걸러냅니다. 사람이 0.3 m/s로 걸어도 0.1 s 동안 3 cm만 움직이므로 0.3 m면 충분히 넉넉합니다.
3. **측정값**: 표면 거리 $r$에 사람 반지름(≈ 0.12 m)을 더해 중심 거리로 만들고, 로봇 자세(`/odom`)로 odom 좌표 $z = (x + (r+R)\cos(θ+a),\ y + (r+R)\sin(θ+a))$로 바꿉니다. 방위는 카메라 값이 아니라 **라이다 빔 각도 a**를 씁니다. 라이다 각도가 더 정확하기 때문입니다.

### 3단계: α-β 필터

사람 위치 $p$와 속도 $v$를 odom 좌표에서 추정합니다. 주기 Δt = 0.1 s(라이다)마다

```text
예측:  p⁻ = p + v·Δt
갱신:  r = z − p⁻                (잔차, 측정이 있을 때만)
       p = p⁻ + α·r
       v = v + (β/Δt)·r
```

측정이 없으면(카메라 미검출·게이트 거부) **예측만** 합니다. 그래서 잠깐 가려져도 추정 위치가 사람의 진행 방향으로 계속 움직입니다. α는 "측정을 얼마나 믿나", β는 "속도를 얼마나 빨리 고치나"입니다. α-β 필터는 등속 모델 칼만 필터가 정상상태에 이르렀을 때의 이득을 상수로 고정한 것과 같습니다. 이 장에서는 α = 0.5, β = 0.1을 씁니다. 칼만 정상상태 관계(Kalata) $β = 2(2−α) − 4\sqrt{1−α}$에 α = 0.5를 넣으면 β ≈ 0.17이므로, 0.1은 그보다 작게 잡아 속도 추정을 **보수적으로**(잡음에 덜 민감하게) 만든 값입니다. 속도는 피드포워드로 바로 모터 명령이 되므로 떨리지 않는 편이 낫습니다.

### 4단계: 제어 법칙

필터가 준 사람 위치로 표면 거리 $d$와 방위 $b$를 다시 계산하고

```text
v  = clip( v_ff + Kp·(d − 0.8),  −0.1, 0.2 )     v_ff = (사람 속도)·(시선 방향 단위벡터)
ω  = clip( Kb·b,  −0.5, 0.5 )                     Kp = 0.8, Kb = 1.5
```

M06에서 본 것처럼 P 제어만으로는 $v_t$로 멀어지는 목표에 $e = v_t / K_p$의 오차가 남습니다(0.15 m/s, Kp 0.8이면 0.19 m). 필터가 추정한 사람 속도의 **시선 성분을 피드포워드**로 더하면 제어기는 "따라잡는 몫"만 담당하므로 오차가 거의 0이 됩니다. 속도 추정이 있어야만 가능한 개선이라는 점이 필터를 쓰는 두 번째 이유입니다.

### 5단계: 상태 기계

@fig[p04-fsm] Follow-me 상태 기계. 안전 조건(사람 0.4 m 미만, 비상 정지)은 모든 상태에서 먼저 검사합니다.

| 상태 | 출력 | 나가는 조건 |
|---|---|---|
| IDLE | 정지 | 오른팔 들기 1 s → 몸통 색 등록 → FOLLOW |
| FOLLOW | 위 제어 법칙 | 1.5 s 미검출 → SEARCH, 양팔 들기 → IDLE, 장애물 → AVOID |
| AVOID | 전진 0, 메카넘은 장애물 반대쪽으로 0.1 m/s 옆걸음, 회전은 유지 | 장애물 해제 → FOLLOW |
| SEARCH | 1.5 s 더 제자리 대기(가림 대비) 후 마지막 방위 쪽으로 0.3 rad/s 회전 | 재발견 + 신원 확인 → FOLLOW, 10 s → IDLE(부저) |

SEARCH에서 곧바로 돌지 않고 잠깐 기다리는 이유는, 가장 흔한 상실 원인이 **짧은 가림**이기 때문입니다. 기둥 뒤로 사라진 사람은 대개 같은 방향에서 다시 나타나므로, 로봇이 먼저 돌아 버리면 오히려 놓칩니다(실습 3에서 확인).

### 신원: 누가 주인인가

MediaPipe의 레거시 Pose(`mp.solutions.pose`)는 **한 사람만** 추적합니다. 화면에 두 사람이 있으면 더 크게 보이는 사람으로 넘어갈 수 있습니다. 그래서 시작 제스처 순간 주인의 **몸통(어깨~엉덩이) HSV 색 히스토그램**을 등록하고, 이후 프레임마다 `cv2.compareHist` 상관계수가 0.5보다 낮으면 "다른 사람"으로 보고 측정을 버립니다. 거리 게이트(연속성)와 색(외형)을 함께 쓰는 셈입니다. 옷 색이 같은 사람이 옆에 있으면 여전히 헷갈리므로, 확장 과제에서 다중 인물 검출과 재식별(re-ID)을 다룹니다.

## 단계별 구현

### M1. 원본 동작 관찰

:::task M1 — 실패 사례 수집
1. 원본 라이다 앱을 추종 모드(`set_running` 2)로 실행하고, 사람 옆에 의자를 두었을 때 로봇이 무엇을 따라가는지 기록합니다.
2. 사람이 0.15 m/s로 걸어서 멀어질 때 로봇이 유지하는 거리를 줄자로 재고, M06의 식 $d ≈ thr/2 + v_t/K_p$와 비교합니다.
3. `body_control`을 바퀴를 띄운 상태로 실행해 Pose가 전신을 잡는 거리(대략 2 m 이상)를 확인합니다. 0.8 m에서는 상반신만 보인다는 점이 중요합니다.
:::

### M2. 방위 교정

`camera_info`에서 fx·cx를 읽고, 1 m 앞 좌우 0.3 m에 사람(또는 상자)을 세워 `b_cam`과 라이다 최근접 각도가 2° 안에서 일치하는지 봅니다. 차이가 일정하면 라이다 장착 각도 오프셋(`LIDAR_YAW`)으로 보정합니다. 체크포인트: 좌·중·우 세 위치에서 두 센서의 방위 차이가 2° 이하다.

### M3. 게이팅과 필터

실습 1로 게이트·필터의 효과를 수치로 확인한 뒤, 실물에서는 필터 없이(측정 그대로) 추종할 때와 필터를 켰을 때의 `/controller/cmd_vel` 떨림을 rosbag으로 비교합니다. 체크포인트: 의자 옆을 지나갈 때 대상이 의자로 넘어가지 않는다.

### M4. 거리 제어와 피드포워드

실습 2에서 `FF`를 1.0과 0.0으로 바꿔 평균 거리 오차를 비교합니다. 실물에서는 0.15 m/s 직선 보행으로 같은 비교를 합니다. 체크포인트: 직선 보행 평균 거리 0.8 ± 0.05 m.

### M5. 상태 기계·탐색·장애물

실습 3의 가림·장애물 시나리오를 실물 복도에서 재현합니다. 기둥 대신 종이 상자를 세우고, 바닥에 작은 상자를 놓아 AVOID를 시험합니다. 체크포인트: 1~3 s 가림 후 재추종 3회 중 3회 성공.

### M6. 평가 코스 리허설

:::task M6 — 평가 코스
1. L자 복도(직선 3 m → 90° 코너 → 직선 2 m)에 기둥(상자) 1개, 바닥 장애물 1개를 둡니다.
2. 주인은 0.2~0.3 m/s로 걷고, 중간에 다른 팀원 한 명이 사이를 가로지릅니다.
3. 평가 기준표의 항목을 각 2회 측정합니다.
:::

:::check 체크포인트 (전체)
- [ ] 제스처 시작·정지가 각 5회 중 5회 동작하고, 오인식으로 시작된 적이 없다.
- [ ] 추정 거리(rosbag)와 줄자 측정이 5 cm 안에서 일치한다.
- [ ] 비상 정지 절차를 팀원 모두 시연했다.
:::

## 실습 (시뮬레이션)

아래 블록은 강의 페이지에서 바로 실행되며, 결과 패널에 로그와 함께 **2D·3D 리플레이**가 자동으로 표시됩니다(실습 2·3). 시뮬레이터에는 카메라가 없으므로, 시뮬 전용 `sim.movers()`(사람의 실제 위치)와 `sim.pose()`(로봇의 실제 자세)로 **MediaPipe가 내놓을 방위**를 만들고, 화각 ±30° 제한·방위 잡음 σ 2°·검출 실패 10%를 더해 현실적인 카메라를 흉내 냅니다. 라이다는 실제 `/scan`을 씁니다.

@btn[~/tools/playground.html] Playground 열기

### 실습 1: 최근접 vs 게이팅 vs 필터

로봇은 가만히 있고 사람이 비스듬히 걸어가며, 사람보다 가까운 곳에 의자 다리가 있습니다. 네 가지 방법의 사람 위치 오차를 비교합니다(ROS 없이 numpy만).

```python run
import math
import numpy as np

rng = np.random.default_rng(0)
ANG = np.radians(np.arange(-180, 180))                    # 1° 간격 라이다 빔 (0 = 전방)
CHAIR = (0.7, 0.15, 0.03)                                 # 의자 다리: 사람보다 가까운 방해물
R_P, GATE, DT = 0.12, math.radians(8), 0.1

def scan(objs):                                           # 원형 물체들에 대한 레이캐스트 + 잡음 1 cm
    r = np.full(len(ANG), np.inf)
    for cx, cy, rad in objs:
        b = -(cx * np.cos(ANG) + cy * np.sin(ANG))
        disc = b * b - (cx * cx + cy * cy - rad * rad)
        t = -b - np.sqrt(np.where(disc >= 0, disc, 0))
        r = np.where((disc >= 0) & (t > 0), np.minimum(r, t), r)
    return r + rng.normal(0, 0.01, len(r))

def to_xy(r, a):                                          # 표면 거리 → 사람 중심 좌표
    return np.array([(r + R_P) * math.cos(a), (r + R_P) * math.sin(a)])

err = {"최근접(M06 방식)": [], "방위 게이트": [], "방위+거리 게이트": [], "+ α-β 필터": []}
p, v, last = None, np.zeros(2), None
for k in range(100):                                      # 10 s, 10 Hz
    truth = np.array([1.0 + 0.06 * k * DT, -0.4 + 0.09 * k * DT])   # 사람이 비스듬히 걸음
    r = scan([CHAIR, (truth[0], truth[1], R_P)])
    i = int(np.argmin(r))
    err["최근접(M06 방식)"].append(np.linalg.norm(to_xy(r[i], ANG[i]) - truth))
    b_cam = math.atan2(truth[1], truth[0]) + rng.normal(0, math.radians(2))
    if rng.random() < 0.1:                                # 카메라 검출 실패 10%
        b_cam = None
    if p is not None:
        p = p + v * DT                                    # α-β 예측
    if b_cam is not None:
        g = np.where(np.abs(ANG - b_cam) < GATE)[0]
        j = g[np.argmin(r[g])]
        z = to_xy(r[j], ANG[j])
        err["방위 게이트"].append(np.linalg.norm(z - truth))
        if last is None or np.linalg.norm(z - last) < 0.3:   # 이전 위치에서 0.3 m 넘게 튀면 거부
            last = z
            if p is None:
                p = z.copy()
            res = z - p
            p, v = p + 0.5 * res, v + 0.1 / DT * res       # α = 0.5, β = 0.1
    if last is not None:
        err["방위+거리 게이트"].append(np.linalg.norm(last - truth))
    if p is not None:
        err["+ α-β 필터"].append(np.linalg.norm(p - truth))

for name, e in err.items():
    e = np.array(e)
    print("%-16s RMS %.3f m  최대 %.3f m  (표본 %d)" % (name, np.sqrt(np.mean(e ** 2)), e.max(), len(e)))
print("추정 속도 (%.3f, %.3f) m/s  ← 실제 (0.060, 0.090)" % tuple(v))
```

```text
최근접(M06 방식)      RMS 0.616 m  최대 0.886 m  (표본 100)
방위 게이트           RMS 0.492 m  최대 0.886 m  (표본 87)
방위+거리 게이트        RMS 0.205 m  최대 0.506 m  (표본 100)
+ α-β 필터         RMS 0.105 m  최대 0.240 m  (표본 100)
추정 속도 (0.042, 0.130) m/s  ← 실제 (0.060, 0.090)
```

- **최근접(M06 방식)**은 거의 항상 의자를 사람으로 잡습니다(RMS 0.6 m). 원본 라이다 추종이 의자 옆에서 엉뚱하게 도는 이유입니다.
- **방위 게이트**만 쓰면 사람이 의자와 같은 방향(약 12°)을 지나는 후반 약 3 s 동안 게이트 안에 의자가 들어와 다시 틀립니다(최대 오차가 최근접과 같음).
- **거리 게이트**를 더하면 갑자기 0.3 m 넘게 가까워진 측정을 버려 오차가 크게 줄고, **α-β 필터**는 거부된 동안에도 속도로 위치를 예측해 RMS 0.1 m까지 내려갑니다.
- 추정 속도가 실제와 다소 다른 것은 의자에 가려진 동안 측정이 끊겼기 때문입니다. `CHAIR`를 (0.7, −0.6)으로 옮겨 방위가 겹치지 않게 하면 네 방법의 차이가 어떻게 변하는지 보세요.

### 실습 2: 게이팅 융합 + α-β 필터 + 피드포워드 추종

`sim.add_walker()`로 사람이 ㄱ자 경로를 0.15 m/s로 걷습니다. 로봇은 10 Hz 라이다 콜백마다 카메라 방위로 게이트를 걸고, odom 좌표에서 α-β 필터를 돌려 0.8 m를 유지합니다.

```python run
import math
import random
import time
import rclpy
from rclpy.node import Node
from geometry_msgs.msg import Twist
from sensor_msgs.msg import LaserScan
from nav_msgs.msg import Odometry
from tf_transformations import euler_from_quaternion
import mentorpi_sim as sim                                          # 시뮬 전용
sim.setup(chassis="mecanum", world="empty", duration=30.0, start=(0.0, 0.0, 0.0))  # 시뮬 전용
sim.add_walker([(0.9, 0.0), (2.2, 0.0), (2.2, 1.5), (0.4, 1.5)], speed=0.15, loop=False)  # 시뮬 전용

FOV, GATE, R_P, D_REF = math.radians(30), math.radians(8), 0.12, 0.8
ALPHA, BETA, FF = 0.5, 0.1, 1.0                                     # α-β 이득, 피드포워드(0이면 끔)
random.seed(1)

def camera_bearing():                                               # 시뮬 전용: MediaPipe 대용
    x, y, th = sim.pose()
    px, py, _ = sim.movers()[0]
    b = math.atan2(py - y, px - x) - th
    b = math.atan2(math.sin(b), math.cos(b))
    if abs(b) > FOV or random.random() < 0.1:                       # 화각 밖 / 10% 검출 실패
        return None
    return b + random.gauss(0.0, math.radians(2.0))                # 영상 x → 방위 오차 σ 2°

class Follower(Node):
    def __init__(self):
        super().__init__("follow_me")
        self.pub = self.create_publisher(Twist, "/cmd_vel", 1)
        self.create_subscription(LaserScan, "/scan", self.on_scan, 1)
        self.create_subscription(Odometry, "/odom", self.on_odom, 10)
        self.odom, self.p, self.v, self.t_seen = (0.0, 0.0, 0.0), None, [0.0, 0.0], 0.0
        self.t0, self.k, self.err = time.time(), 0, []

    def on_odom(self, m):
        q = m.pose.pose.orientation
        self.odom = (m.pose.pose.position.x, m.pose.pose.position.y,
                     euler_from_quaternion([q.x, q.y, q.z, q.w])[2])

    def on_scan(self, msg):                                         # 10 Hz: 융합 + 제어
        dt, now = 0.1, time.time()
        x, y, th = self.odom
        if self.p:                                                  # 예측
            self.p = [self.p[0] + self.v[0] * dt, self.p[1] + self.v[1] * dt]
        b_cam = camera_bearing()
        if b_cam is not None:                                       # 카메라 방위로 라이다 게이팅
            best = None
            for i, r in enumerate(msg.ranges):
                a = msg.angle_min + i * msg.angle_increment
                if abs(a - b_cam) < GATE and math.isfinite(r) and (best is None or r < best[0]):
                    best = (r, a)
            if best:
                r, a = best
                z = (x + (r + R_P) * math.cos(th + a), y + (r + R_P) * math.sin(th + a))
                if self.p is None:
                    self.p = list(z)
                res = (z[0] - self.p[0], z[1] - self.p[1])
                self.p = [self.p[0] + ALPHA * res[0], self.p[1] + ALPHA * res[1]]
                self.v = [self.v[0] + BETA / dt * res[0], self.v[1] + BETA / dt * res[1]]
                self.t_seen = now
        cmd = Twist()
        if self.p and now - self.t_seen < 1.5:
            dx, dy = self.p[0] - x, self.p[1] - y
            d = math.hypot(dx, dy) - R_P                             # 표면까지 거리
            b = math.atan2(dy, dx) - th
            b = math.atan2(math.sin(b), math.cos(b))
            ff = (self.v[0] * dx + self.v[1] * dy) / math.hypot(dx, dy)   # 목표 속도의 시선 성분
            cmd.linear.x = max(-0.1, min(0.2, FF * ff + 0.8 * (d - D_REF)))
            cmd.angular.z = max(-0.5, min(0.5, 1.5 * b))
            gx, gy, _ = sim.movers()[0]                              # 시뮬 전용: 실제 거리
            rx, ry, _ = sim.pose()                                   # 시뮬 전용
            true_d = math.hypot(gx - rx, gy - ry) - R_P
            if now - self.t0 > 5:
                self.err.append(true_d - D_REF)
            if self.k % 30 == 0:
                print("t=%4.1f 실제 %.2f m 추정 %.2f m 방위 %+5.1f° v=%+.2f ω=%+.2f"
                      % (now - self.t0, true_d, d, math.degrees(b), cmd.linear.x, cmd.angular.z))
        self.k += 1
        self.pub.publish(cmd)

rclpy.init()
node = Follower()
try:
    rclpy.spin(node)
except KeyboardInterrupt:
    pass
finally:
    node.pub.publish(Twist())
    e = node.err
    print("5 s 이후 거리 오차: 평균 %+.3f m, RMS %.3f m" % (sum(e) / len(e), math.sqrt(sum(v * v for v in e) / len(e))))
    node.destroy_node()
    rclpy.shutdown()
```

(대략)

```text
t= 0.0 실제 0.78 m 추정 0.78 m 방위  +0.0° v=-0.02 ω=+0.00
t= 3.0 실제 0.82 m 추정 0.80 m 방위  -0.5° v=+0.15 ω=-0.01
t= 6.0 실제 0.81 m 추정 0.80 m 방위  -0.3° v=+0.15 ω=-0.01
t= 9.0 실제 0.76 m 추정 0.76 m 방위  +2.5° v=+0.04 ω=+0.06
t=12.0 실제 0.81 m 추정 0.80 m 방위  +7.4° v=+0.09 ω=+0.19
t=15.0 실제 0.81 m 추정 0.80 m 방위  +4.8° v=+0.12 ω=+0.13
t=18.0 실제 0.81 m 추정 0.80 m 방위  +4.5° v=+0.16 ω=+0.12
t=21.0 실제 0.79 m 추정 0.78 m 방위  +7.0° v=-0.00 ω=+0.18
t=24.0 실제 0.81 m 추정 0.80 m 방위  +4.6° v=+0.07 ω=+0.12
t=27.0 실제 0.81 m 추정 0.80 m 방위  +4.5° v=+0.13 ω=+0.12
[sim] 시뮬레이션 시간 30.0 s 종료 (sim.setup(duration=...)으로 변경)
5 s 이후 거리 오차: 평균 -0.001 m, RMS 0.019 m
```

- 추정 거리와 실제 거리가 1~2 cm 안에서 일치합니다. 라이다가 거리를 재므로 카메라만으로 거리를 추정할 때(몸 크기 → 거리)보다 한 자릿수 정확합니다.
- 코너(약 9 s, 19 s 이후)에서 방위가 +5° 안팎으로 유지되는 것은 사람이 계속 왼쪽으로 돌아 나가는 중이기 때문이고, ω가 그만큼 따라 돕니다.
- `FF`를 0.0으로 바꿔 실행하면 5 s 이후 평균 거리 오차가 약 **+0.11 m**(0.91 m에서 추종)로 커집니다. 이론값 $v_t/K_p = 0.15/0.8 ≈ 0.19$ m보다 작은 것은 코너에서 사람과 로봇 경로가 달라 거리가 줄어드는 구간이 섞이기 때문입니다.

### 실습 3: 상태 기계 — 제스처·가림·탐색·장애물

사람이 기둥(상자) 뒤로 돌아가 카메라에서 **가려지고**, 다시 나타난 뒤 로봇 앞에 화분(원통)이 있는 시나리오입니다. 가림은 "카메라 방향의 라이다 거리가 사람보다 0.15 m 넘게 짧으면 안 보임"으로 흉내 냅니다. 제스처는 시각표(`GESTURES`)로 대신합니다.

```python run
import math
import random
import time
import rclpy
from rclpy.node import Node
from geometry_msgs.msg import Twist
from sensor_msgs.msg import LaserScan
import mentorpi_sim as sim                                          # 시뮬 전용
sim.setup(chassis="mecanum", world="empty", duration=30.0, start=(0.0, 0.0, 0.0))  # 시뮬 전용
sim.add_walker([(0.9, 0.0), (1.9, 0.0), (1.9, -1.4), (0.6, -1.4)], speed=0.2, loop=False)  # 시뮬 전용
sim.add_box(1.45, -0.55, 0.3, 0.3)                                  # 시뮬 전용: 기둥(사람을 가림)
sim.add_cylinder(0.7, -0.55, 0.07)                                  # 시뮬 전용: 바닥의 화분(장애물)

FOV, GATE, R_P, D_REF = math.radians(30), math.radians(8), 0.12, 0.8
GESTURES = [(1.0, "right_arm_up"), (27.0, "both_arms_up")]          # 시뮬 전용: 제스처 대본
random.seed(2)

def ang(a):
    return math.atan2(math.sin(a), math.cos(a))

def camera(scan):                                                   # 시뮬 전용: MediaPipe 대용
    x, y, th = sim.pose()
    px, py, _ = sim.movers()[0]
    b, dist = ang(math.atan2(py - y, px - x) - th), math.hypot(px - x, py - y)
    i = int(round((b - scan.angle_min) / scan.angle_increment)) % len(scan.ranges)
    if abs(b) > FOV or scan.ranges[i] < dist - R_P - 0.15 or random.random() < 0.1:
        return None                                                 # 화각 밖 / 가려짐 / 검출 실패
    return b + random.gauss(0.0, math.radians(2.0))

class FollowFSM(Node):
    def __init__(self):
        super().__init__("follow_fsm")
        self.pub = self.create_publisher(Twist, "/cmd_vel", 1)
        self.create_subscription(LaserScan, "/scan", self.on_scan, 1)
        self.state, self.t0, self.t_seen, self.b_last = "IDLE", time.time(), -9.0, 0.0
        self.d, self.n_block, self.t_block = None, 0, 0.0

    def go(self, s, why):
        print("%5.1fs %-6s → %-6s (%s)" % (time.time() - self.t0, self.state, s, why))
        self.state = s

    def on_scan(self, msg):
        t = time.time() - self.t0
        if GESTURES and t >= GESTURES[0][0]:                        # 오른팔 들기=시작, 양팔 들기=정지
            g = GESTURES.pop(0)[1]
            self.go("FOLLOW" if g == "right_arm_up" else "IDLE", "제스처 " + g)
        beams = [(msg.angle_min + i * msg.angle_increment, r) for i, r in enumerate(msg.ranges)]
        rng = lambda a0, a1: min([r for a, r in beams if a0 <= a <= a1] + [9.9])
        b = camera(msg)
        if b is not None:
            r = rng(b - GATE, b + GATE)                             # 게이트 안 최근접 = 사람
            if t - self.t_seen > 1.5 or abs(r - self.d) < 0.3:     # 추적 중엔 거리도 게이팅(가림막 거부)
                self.d = r if t - self.t_seen > 1.5 else 0.5 * self.d + 0.5 * r   # 재발견이면 새로 시작
                self.b_last, self.t_seen = b, t
        lost = t - self.t_seen
        if self.state == "FOLLOW" and lost > 1.5:
            self.go("SEARCH", "1.5 s 동안 안 보임")
        elif self.state == "SEARCH" and lost < 0.1:
            self.go("FOLLOW", "재발견")
        elif self.state == "SEARCH" and lost > 10.0:
            self.go("IDLE", "탐색 실패")
        cmd = Twist()
        if self.state == "FOLLOW" and lost < 1.5:
            obs = min([(r, a) for a, r in beams if abs(a) < math.radians(35)
                       and abs(a - self.b_last) > GATE] + [(9.9, 0.0)])  # 사람 게이트를 뺀 전방
            cmd.angular.z = max(-0.5, min(0.5, 1.5 * self.b_last))
            cmd.linear.x = max(-0.1, min(0.2, 0.8 * (self.d - D_REF)))
            if obs[0] < 0.35 and cmd.linear.x > 0:                  # 사람 아닌 장애물이 가까움
                cmd.linear.x = 0.0                                  # 전진 멈추고 옆으로 비켜 감(메카넘)
                cmd.linear.y = -math.copysign(0.1, obs[1])
                if self.n_block == 0 or t - self.t_block > 0.5:
                    print("%5.1fs 장애물 %.2f m (%+.0f°) → 옆으로 비킴" % (t, obs[0], math.degrees(obs[1])))
                self.n_block, self.t_block = self.n_block + 1, t
        elif self.state == "SEARCH" and lost > 3.0:                 # 1.5 s 더 기다린 뒤 마지막 방향으로 회전
            cmd.angular.z = math.copysign(0.3, self.b_last)
        self.pub.publish(cmd)

rclpy.init()
node = FollowFSM()
try:
    rclpy.spin(node)
except KeyboardInterrupt:
    pass
finally:
    node.pub.publish(Twist())
    x, y, _ = sim.pose()                                            # 시뮬 전용
    px, py, _ = sim.movers()[0]                                     # 시뮬 전용
    print("최종 상태 %s, 사람까지 %.2f m, 장애물 회피 %.1f s"
          % (node.state, math.hypot(px - x, py - y) - R_P, node.n_block * 0.1))
    node.destroy_node()
    rclpy.shutdown()
```

(대략)

```text
  1.0s IDLE   → FOLLOW (제스처 right_arm_up)
  9.2s FOLLOW → SEARCH (1.5 s 동안 안 보임)
 12.6s SEARCH → FOLLOW (재발견)
 16.6s 장애물 0.35 m (-34°) → 옆으로 비킴
 27.0s FOLLOW → IDLE   (제스처 both_arms_up)
[sim] 시뮬레이션 시간 30.0 s 종료 (sim.setup(duration=...)으로 변경)
최종 상태 IDLE, 사람까지 0.81 m, 장애물 회피 2.2 s
```

- 9.2 s: 사람이 기둥 뒤로 가려 1.5 s 동안 안 보이자 SEARCH로 갑니다. 1.5 s 더 기다린 뒤 마지막 방위 쪽으로 돌고, 12.6 s에 기둥 옆으로 나온 사람을 재발견합니다.
- 추적 중에는 **거리 게이트**가 기둥 모서리를 거부합니다. 이 줄(`abs(r - self.d) < 0.3`)을 지우고 실행하면, 가려지는 순간 기둥까지의 거리를 사람 거리로 믿고 로봇이 뒤로 물러나는 것을 리플레이에서 볼 수 있습니다.
- 16.6 s: 사람 게이트 밖(−34°)의 화분이 0.35 m 안에 들어오자 전진을 멈추고 반대쪽(왼쪽)으로 옆걸음합니다. 메카넘이라 방향을 틀지 않고 비킬 수 있습니다.
- SEARCH의 대기 조건(`lost > 3.0`)을 지우고 곧바로 돌게 하면 가려진 동안 로봇이 사람 반대쪽으로 돌아 버려 탐색에 실패하는 경우가 생깁니다. 시드(`random.seed`)를 바꿔 여러 번 실행해 보세요.

2D 시뮬레이터의 **라이다 추종** 모드와 비교해 보는 것도 좋습니다. 추종 모드는 최근접점을 따라가므로, 사람 근처에 장애물을 두면 대상이 바뀌는 현상을 바로 볼 수 있습니다.

@btn[~/sim/index.html] 2D 시뮬레이터 열기

## 실습 (실물)

:::safety 실물 추종 안전 수칙
- 추종 대상 외의 사람은 로봇 주변 1 m 밖에 있습니다. 로봇 뒤쪽은 라이다 일부만 보므로 **후진은 0.1 m/s로 제한**합니다.
- 처음에는 바퀴를 띄운 상태에서 제스처·방위 부호(사람이 왼쪽이면 ω > 0)를 확인하고, 다음에 넓은 곳에서 **0.1 m/s 상한**으로 시험한 뒤 0.2 m/s로 올립니다.
- 주인은 항상 로봇을 마주 볼 수 있어야 하고, 계단·문턱·유리벽이 있는 곳에서는 시험하지 않습니다(라이다가 유리를 못 봄).
- 비상 정지: 양팔 들기(IDLE) → 노드 터미널 `Ctrl+C` → 전원 스위치. 제스처가 안 먹힐 때를 대비해 한 사람은 항상 터미널 앞에 있습니다.
:::

### 1. 원본 동작 확인(M1)

```bash
~/.stop_ros.sh
ros2 launch app lidar_node.launch.py debug:=true
# 다른 터미널
ros2 service call /lidar_app/enter std_srvs/srv/Trigger {}
ros2 service call /lidar_app/set_param interfaces/srv/SetFloat64List "{data: [1.0, 90.0, 0.15]}"
ros2 service call /lidar_app/set_running interfaces/srv/SetInt64 "{data: 2}"     # 2 = 추종
ros2 service call /lidar_app/exit std_srvs/srv/Trigger {}
```

threshold 1.0 m면 목표 거리는 0.5 m입니다(M06). 끝나면 `exit`로 정지합니다. MediaPipe Pose 동작은 원본 자세 제어로 확인합니다(**섀시가 움직이므로 바퀴를 띄움**).

```bash
ros2 launch example body_control.launch.py
```

### 2. 교정과 팀 노드 실행

```bash
~/.stop_ros.sh
ros2 launch peripherals depth_camera.launch.py          # 터미널 1
ros2 launch peripherals lidar.launch.py                 # 터미널 2
ros2 launch controller controller.launch.py             # 터미널 3
ros2 topic echo --once /ascamera/camera_publisher/rgb0/camera_info | grep -A1 "^k:"   # fx, cx 확인
python3 ~/follow_me.py                                  # 터미널 4
ros2 topic echo /follow_me/state                        # 전이 로그
```

### 3. 팀 노드 코드 (`follow_me.py`)

메카넘 기준 골격입니다. `FX`, `CX`는 `camera_info` 값으로, `LIDAR_YAW`는 M2 교정 값으로 바꿉니다. 애커만은 AVOID의 옆걸음 대신 정지만 하고, ω를 조향각으로 바꿔야 합니다(확장 과제).

```python robot
#!/usr/bin/env python3
import math
import cv2
import numpy as np
import mediapipe as mp
import rclpy
from rclpy.node import Node
from rclpy.qos import qos_profile_sensor_data
from cv_bridge import CvBridge
from sensor_msgs.msg import Image, LaserScan
from geometry_msgs.msg import Twist
from nav_msgs.msg import Odometry
from std_msgs.msg import String
from tf_transformations import euler_from_quaternion

FX, CX, LIDAR_YAW = 554.0, 320.0, 0.0          # camera_info K[0], K[2] / 라이다 각도 오프셋 [rad]
GATE, R_GATE, R_P, D_REF, D_MIN = math.radians(8), 0.3, 0.12, 0.8, 0.4
ALPHA, BETA, KP, KB = 0.5, 0.1, 0.8, 1.5
LM = mp.solutions.pose.PoseLandmark

class FollowMe(Node):
    def __init__(self):
        super().__init__('follow_me')
        self.pose = mp.solutions.pose.Pose(min_detection_confidence=0.7, min_tracking_confidence=0.7)
        self.bridge = CvBridge()
        self.state, self.b_cam, self.t_cam, self.t_seen, self.t_in = "IDLE", None, 0.0, -9.0, 0.0
        self.p, self.v, self.odom, self.owner, self.cnt = None, np.zeros(2), (0.0, 0.0, 0.0), None, [0, 0]
        self.pub = self.create_publisher(Twist, '/controller/cmd_vel', 1)
        self.state_pub = self.create_publisher(String, '~/state', 10)
        self.create_subscription(Image, '/ascamera/camera_publisher/rgb0/image', self.on_image, 1)
        self.create_subscription(LaserScan, '/scan_raw', self.on_scan, qos_profile_sensor_data)
        self.create_subscription(Odometry, '/odom', self.on_odom, 10)

    def now(self):
        return self.get_clock().now().nanoseconds * 1e-9

    def go(self, s, why):
        self.get_logger().info('%s --%s--> %s' % (self.state, why, s))
        self.state, self.t_in = s, self.now()
        self.state_pub.publish(String(data=s))

    def on_odom(self, m):
        q = m.pose.pose.orientation
        self.odom = (m.pose.pose.position.x, m.pose.pose.position.y,
                     euler_from_quaternion([q.x, q.y, q.z, q.w])[2])

    def on_image(self, msg):                                    # MediaPipe: 방위·제스처·신원
        rgb = self.bridge.imgmsg_to_cv2(msg, "rgb8")
        h, w = rgb.shape[:2]
        res = self.pose.process(rgb)
        if not res.pose_landmarks:
            return
        lm = res.pose_landmarks.landmark
        xs = [lm[i].x * w for i in (LM.LEFT_SHOULDER, LM.RIGHT_SHOULDER, LM.LEFT_HIP, LM.RIGHT_HIP)]
        ys = [lm[i].y * h for i in (LM.LEFT_SHOULDER, LM.RIGHT_SHOULDER, LM.LEFT_HIP, LM.RIGHT_HIP)]
        x0, x1 = int(max(0, min(xs))), int(min(w, max(xs)))
        y0, y1 = int(max(0, min(ys))), int(min(h, max(ys)))
        if x1 - x0 < 10 or y1 - y0 < 10:
            return
        hsv = cv2.cvtColor(rgb[y0:y1, x0:x1], cv2.COLOR_RGB2HSV)
        hist = cv2.normalize(cv2.calcHist([hsv], [0, 1], None, [16, 8], [0, 180, 0, 256]), None)
        r_up = lm[LM.RIGHT_WRIST].y < lm[LM.NOSE].y             # 손목이 코보다 위
        l_up = lm[LM.LEFT_WRIST].y < lm[LM.NOSE].y
        self.cnt = [self.cnt[0] + 1 if r_up and not l_up else 0, self.cnt[1] + 1 if r_up and l_up else 0]
        if self.state == "IDLE" and self.cnt[0] >= 15:          # 약 1 s (15 fps 가정)
            self.owner, self.cnt = hist, [0, 0]
            self.go("FOLLOW", "오른팔 들기 → 주인 등록")
        elif self.state != "IDLE" and self.cnt[1] >= 10:
            self.go("IDLE", "양팔 들기")
        if self.owner is None or cv2.compareHist(self.owner, hist, cv2.HISTCMP_CORREL) < 0.5:
            return                                              # 주인이 아니면 측정 없음
        u = (lm[LM.LEFT_SHOULDER].x + lm[LM.RIGHT_SHOULDER].x) / 2 * w
        self.b_cam, self.t_cam = -math.atan((u - CX) / FX), self.now()

    def on_scan(self, msg):                                     # 10 Hz: 융합 + 상태 기계 + 제어
        dt, now = 0.1, self.now()
        x, y, th = self.odom
        a = msg.angle_min + LIDAR_YAW + np.arange(len(msg.ranges)) * msg.angle_increment
        a = np.arctan2(np.sin(a), np.cos(a))
        r = np.array(msg.ranges)
        r[~np.isfinite(r) | (r < 0.05)] = 99.0
        if self.p is not None:
            self.p = self.p + self.v * dt                       # α-β 예측
        if self.b_cam is not None and now - self.t_cam < 0.2:
            idx = np.where(np.abs(a - self.b_cam) < GATE)[0]
            if len(idx):
                j = idx[np.argmin(r[idx])]
                z = np.array([x + (r[j] + R_P) * math.cos(th + a[j]), y + (r[j] + R_P) * math.sin(th + a[j])])
                fresh = now - self.t_seen > 1.5
                r_hat = None if self.p is None else math.hypot(*(self.p - (x, y))) - R_P
                if fresh or r_hat is None:                      # 상실 후 첫 측정: 트랙 새로 시작
                    self.p, self.v, self.t_seen = z, np.zeros(2), now
                elif abs(r[j] - r_hat) < R_GATE:                # 거리 게이트 통과: α-β 갱신
                    res = z - self.p
                    self.p, self.v, self.t_seen = self.p + ALPHA * res, self.v + BETA / dt * res, now
        lost = now - self.t_seen
        if self.state == "FOLLOW" and lost > 1.5:
            self.go("SEARCH", "1.5 s 미검출")
        elif self.state == "SEARCH" and lost < 0.2:
            self.go("FOLLOW", "재발견")
        elif self.state == "SEARCH" and now - self.t_in > 10.0:
            self.go("IDLE", "탐색 실패")                         # 부저 알림은 확장 과제
        t = Twist()
        if self.state == "FOLLOW" and self.p is not None:
            dx, dy = self.p - (x, y)
            d, b = math.hypot(dx, dy) - R_P, math.atan2(dy, dx) - th
            b = math.atan2(math.sin(b), math.cos(b))
            v_ff = float(self.v @ (dx, dy)) / max(math.hypot(dx, dy), 1e-3)
            t.linear.x = float(np.clip(v_ff + KP * (d - D_REF), -0.1, 0.2))
            t.angular.z = float(np.clip(KB * b, -0.5, 0.5))
            side = (np.abs(a) < math.radians(35)) & (np.abs(a - b) > GATE)
            if side.any() and r[side].min() < 0.35 and t.linear.x > 0:   # AVOID: 전진 멈춤 + 옆걸음
                t.linear.x = 0.0
                t.linear.y = -math.copysign(0.1, a[side][np.argmin(r[side])])
            if d < D_MIN:
                t.linear.x = min(t.linear.x, 0.0)               # 너무 가까우면 전진 금지
        elif self.state == "SEARCH" and lost > 3.0 and self.b_cam is not None:
            t.angular.z = math.copysign(0.3, self.b_cam)
        self.pub.publish(t)

def main():
    rclpy.init()
    node = FollowMe()
    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    finally:
        node.pub.publish(Twist())
        node.destroy_node()
        rclpy.shutdown()

if __name__ == '__main__':
    main()
```

코드에서 짚을 점:

- 카메라 콜백은 **방위·제스처·신원만** 만들고, 제어는 라이다 콜백(10 Hz) 한 곳에서 합니다. 카메라 프레임률이 흔들려도 제어 주기는 일정하고, 0.2 s보다 오래된 방위는 쓰지 않습니다.
- 1.5 s 넘게 못 본 뒤 들어온 측정(`fresh`)은 거리 게이트 없이 받아 **트랙을 새로 시작**합니다. 그러지 않으면 사람이 멀리 이동한 뒤에는 영원히 게이트에 걸려 다시 못 찾습니다(실습 3에서 겪는 문제).
- AVOID는 별도 상태 변수 없이 FOLLOW 안에서 명령만 바꿉니다. 로그로 구분하고 싶으면 `go("AVOID", …)`를 추가하세요.
- MediaPipe Pose는 Raspberry Pi 5 CPU에서 수~십수 fps입니다. `model_complexity=0`을 주면 빨라지고, 영상 크기를 줄이면 fx·cx도 같은 비율로 줄여야 합니다.

:::check 체크포인트 (실물)
- [ ] 사람이 로봇 왼쪽에 서면 `b_cam > 0`, ω > 0이다(부호 확인).
- [ ] 의자 옆을 지나도 대상이 바뀌지 않는다(원본 추종과 비교 영상).
- [ ] 다른 사람이 가로질러도 주인을 유지했다(또는 실패 원인을 설명할 수 있다).
:::

## 평가 기준

| 영역 | 기준 | 배점 |
|---|---|---|
| 직선 추종 | 0.2 m/s 보행 3 m, 평균 거리 0.8 ± 0.1 m 15점 / ± 0.2 m 8점 | 15 |
| 코너 추종 | 90° 코너 통과 후에도 추종 유지(2회 중 성공 횟수 × 5) | 10 |
| 가림 복구 | 기둥 뒤 1~3 s 가림 후 재추종(2회 × 5) | 10 |
| 신원 유지 | 다른 사람이 가로지를 때 주인 유지(2회 × 5) | 10 |
| 장애물 | 바닥 장애물 접촉 없이 정지·회피, 사람과 0.4 m 미만 접근 0회 | 10 |
| 제스처 | 시작·정지 각 5회 성공률(오인식 1회당 −2) | 5 |
| 융합 설계·검증 | 게이트·필터 파라미터 근거, 최근접 방식 대비 오차 비교 데이터 | 15 |
| 안전·코드 | 속도 제한, 비상 정지 시연, 파라미터 분리, 상태 로그 | 15 |
| 발표·회고 | 실패 분석과 개선 방향 | 10 |
| **합계** | | **100** |

## 확장 과제

- **칼만 필터로 교체**: α-β 대신 등속 모델 칼만 필터(상태 [x, y, vx, vy], 측정 잡음은 라이다 σ와 방위 σ에서 유도)를 쓰고, 공분산으로 게이트 크기를 자동으로 정하세요(마할라노비스 거리 게이팅).
- **다리 검출**: 라이다 점을 군집화해 "두 개의 작은 원호 = 다리"를 찾으면 카메라가 못 보는 뒤쪽·화각 밖에서도 사람 후보를 유지할 수 있습니다.
- **깊이 카메라 추가**: 어깨 중점의 깊이 값(ascamera depth)을 세 번째 측정으로 넣어, 라이다 높이(약 20 cm)에 걸리지 않는 사람 상체까지 쓰세요.
- **Nav2 연동**: 주인이 멀어지면 사람 위치를 목표로 `NavigateToPose`를 보내 장애물 사이를 경로 계획으로 따라가게 하세요(M11~M13). DWA의 사람 비용(social cost)도 조사해 보세요.
- **애커만 대응**: 제자리 회전이 안 되므로 SEARCH를 전진+조향 원 그리기로 바꾸고, 최소 회전 반경을 고려해 추종 거리를 늘리세요.

## 자주 나는 오류와 해결

| 증상 | 원인 | 해결 |
|---|---|---|
| 사람 쪽이 아니라 반대로 돔 | 방위 부호(영상 반전 좌표 사용, `−atan` 누락) | 반전 전 좌표로 `b = −atan((u−cx)/fx)`, 왼쪽에 섰을 때 b > 0 확인 |
| 추정 거리가 늘 20~30 cm 틀림 | 라이다·카메라 방위 오프셋, 게이트가 벽을 잡음 | M2 교정으로 `LIDAR_YAW` 설정, 게이트 8° → 6° |
| 가까이 오면 추종이 끊김 | 0.8 m에서는 상반신만 보여 Pose 검출 실패, 어깨가 화면 밖 | 카메라를 위로 틀거나 거리 1.0 m, `min_detection_confidence` 0.5 |
| 멈춰 있어도 앞뒤로 흔들림 | 필터 β가 커서 속도 추정이 잡음에 반응 → 피드포워드 떨림 | β 0.1 → 0.05, 사람 속도 0.05 m/s 미만은 0으로 |
| 한번 놓치면 다시 못 찾음 | 거리 게이트가 옛 예측에 묶임 | 상실 후 첫 측정은 게이트 없이 트랙 재시작(`fresh`) |
| 다른 사람을 따라감 | Pose가 한 사람만 추적, 색 히스토그램 문턱이 낮음 | 문턱 0.5 → 0.7, 옷 색이 뚜렷한 조끼 착용 |
| 벽 옆에서 AVOID가 계속 켜짐 | ±35° 섹터가 벽을 잡음 | 섹터를 ±25°로, 벽과 0.5 m 이상 떨어진 경로 |
| `/scan_raw` 콜백이 안 불림 | QoS 불일치 | `qos_profile_sensor_data` 사용 (M05) |
| 로봇이 너무 느리게 반응 | MediaPipe 지연으로 `b_cam`이 0.2 s보다 자주 오래됨 | `model_complexity=0`, 영상 축소, 유효 시간 0.3 s |

## 과제

:::task 과제
1. **(기본)** 실습 1에서 `GATE`를 4°, 8°, 15°로 바꿔 네 방법의 RMS를 표로 정리하고, 게이트 폭의 trade-off(너무 좁으면? 너무 넓으면?)를 설명하세요.
2. **(기본)** 실습 2에서 α를 0.2, 0.5, 0.9로 바꿔 평균·RMS 거리 오차를 비교하세요. α가 1이면 필터가 무엇과 같아지는지 설명합니다.
3. **(심화)** 실습 3의 거리 게이트 기준을 `self.d`(저역 통과 거리) 대신 **예측 위치**에서 계산하도록 바꾸고(실습 2처럼 odom 좌표 필터 사용), 가림 전후 동작이 어떻게 달라지는지 리플레이로 비교하세요.
4. **(심화)** 실물에서 두 사람이 교차하는 장면을 rosbag으로 녹화하고, 색 히스토그램 상관계수의 시간 변화를 그래프로 그려 문턱값을 데이터로 정하세요.
:::

## 참고자료

- 원본 교재: *10. Lidar Lesson* — Lesson 4 Lidar Following, *15. MediaPipe Human-robot Interaction* — Lesson 3 Hand Following, Lesson 4 Posture Control
- 원본 소스: `src/app/app/lidar_controller.py`(running_mode 2), `src/app/launch/lidar_node.launch.py`, `src/example/example/body_control/include/body_control.py`, `body_track.py`, `body_track.launch.py`, `src/example/example/hand_track/hand_detect_node.py`, `hand_track_node.py`
- 이 강의: A05(MediaPipe), A06(자세 제어), M05(라이다 데이터), M06(라이다 추종), M12(DWA·코스트맵)
- MediaPipe Pose Landmarker: https://ai.google.dev/edge/mediapipe/solutions/vision/pose_landmarker
- Kalata, P. R., *The Tracking Index: A Generalized Parameter for α-β and α-β-γ Target Trackers*, IEEE TAES (1984)
- Bar-Shalom, Li & Kirubarajan, *Estimation with Applications to Tracking and Navigation* — 게이팅과 추적 필터
- OpenCV — Histogram Comparison: https://docs.opencv.org/4.x/d8/dc8/tutorial_histogram_comparison.html
- ROS 2 Humble — rosbag2: https://docs.ros.org/en/humble/Tutorials/Beginner-CLI-Tools/Recording-And-Playing-Back-Data/Recording-And-Playing-Back-Data.html
